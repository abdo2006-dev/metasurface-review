## v1.12b — 22 August 2026 — manuscript v0.28a, Akram source-level correction and mirror sync

- **CH-151 ‼★ — the sub-0.1 ms Akram figure is not a per-element measurement, and this project said it was.** The primary source [4] was re-opened. "0.1 ms" occurs **once**, in the abstract: *"…with each element controlled through an FPGA achieving an update time below 0.1 ms."* The only body-text quantities that support it are on **p. 6**: *"For the 16 × 16 RIS tile, once a configuration is stored in the FPGA, the internal update is completed in approximately 40 ns. Including the external control link, the end-to-end reconfiguration time is about 40 μs when using the LAN or Wi-Fi (Ethernet-based) interfaces, and up to 100 μs when using the USB/UART serial interface."* **100 μs = 0.1 ms**, and the object is **one 16 × 16 tile**, not one element; the strings "per-element" and "per element" do not occur in the paper. The second bound, *"The tile ON-OFF configuration can be updated in less than 10 ms"* (p. 8, repeated p. 9), concerns **the same object**. Both end at the configuration write with **no electromagnetic criterion**, and as upper bounds they are mutually consistent. **The element-versus-tile contrast was this project's reading, not the source's, and is withdrawn.** It had propagated into the abstract-adjacent §1 narrative, §4.2, Table 2, Table 3, Table 5, the professor brief, Figure 2's axis labels, S1, S4, `timescale_matrix.md` and `evidence_matrix.md`; all are corrected. The element-versus-surface point the manuscript needs is carried instead by [8], where it is measured (≈30 ms ribbon against ≈300 ms surface).
- **CH-152 ★ — AKR-26 S6 recoded `Q·T` → `Q·(T)`; its measured-duration count falls from 1 to 0.** S6 is defined as the meta-atom electromagnetic state change. AKR-26 measures none: full-text search returns no "switching time", no "rise time" and no "settl-", and PIN diodes are described only qualitatively as offering "fast electronic tuning capabilities" (p. 2). What the source supplies is a duration bounding an aggregate that *contains* S6's electrical write, which is `·(T)` under the v0.24 axis-B definitions. **The three C1 maxima are unchanged** — max Q = 6 (BAI-22, LI-25), max `·T` = 4 (BAI-22), max `·T`+`·(T)` = 6 (LI-25) — because AKR-26 was never at any of them. §4.6 now reads "one measured stage duration each for [5] and [8]; none for [4]". S5 keeps `Q·[R]` alone: the p. 6 aggregate includes the external link, but the authors apportion no share to it and we derive none. Table 3's S6 row and its "sub-millisecond per-element budget" gloss were rewritten accordingly; **no semiconductor transition time is inferred anywhere.**
- **CH-153 — the "harmless" characterisation was too categorical.** §1 and professor brief §2 now read that in quasi-static conditions the abstraction *may be less consequential for system-level performance*, and that under high mobility its timing assumptions *become potentially load-bearing*. No discussion was added around it.
- **CH-154 ★ — the public mirror was stale at v0.23 and its generator was the reason.** `sync_review_repo.py` still declared `MANUSCRIPT_VERSION = "v0.23"`, and its `SECTION_ORDER` was the **twelve-section list**, which drives both `COMPLETE_MANUSCRIPT.md` and the duplicate-section safety check — a sync would have emitted a public manuscript with every section missing. Rebuilt to the seven-section order, with an assertion that `SECTION_ORDER` matches `Documentation/manuscript/` exactly and exits rather than emitting a manuscript with holes in it. The `CURRENT_VERSION.md` freeze-status prose, frozen at v0.23 and citing "evidence base frozen at documentation v1.6", is now derived from the live counts.
- **CH-155 ★ — C2 and C4 were hardcoded in `REVIEW_INDEX.md.tmpl`, and C2 was scientifically wrong after CH-151.** It stated that "an element-level update time cannot be substituted for a tile- or aperture-level configuration time — the same panel reports both". C1 and C3 were already derived from the manuscript; C2 and C4 are now derived the same way, so the mirror cannot restate a superseded contribution. `README.md` no longer says "twelve numbered sections" and `REVIEW_INDEX.md`'s reading order is generated from `SECTION_ORDER`, both via new `{{SECTION_COUNT_WORD}}` and `{{READING_ORDER}}` substitutions.
- **CH-156 — `SOURCE_VERSION_MAP.md`'s per-reference section column is now derived.** All 34 rows carried hand-maintained section numbers from the twelve-section manuscript. The column is regenerated from the manuscript and supplement at each sync; a reference cited only in the supplementary material renders as **S**, which is how [33] now appears.

## v1.12 — 22 August 2026 — manuscript v0.28a, professor-readiness audit

**A narrow pre-send pass, not a revision.** The seven-section structure and every scientific finding are unchanged; C1, C3, C4 and G1–G4 were recomputed and are identical. Full detail in `professor_readiness_v0.28a.md`; v0.28 is preserved at `Documentation/archive/manuscript_v0.28/` and at commit `d850b04`.

- **CH-141 ★ — the figures were numbered 1, 3, 2, 4.** The timing landscape appeared first (§4.5) carrying number 3, the adaptation-chain map second (§4.6) carrying number 2. Swapped so numbering follows first appearance: `fig3_timing_landscape` → `fig2_timing_landscape`, `fig2_adaptation_chain` → `fig3_adaptation_chain`. Captions, in-text references, both PDF/PNG pairs, `make_figures.py`, the exporter's `FIGFILE` map, the mirror whitelist in `sync_review_repo.py`, `manuscript_argument_map.md` and `timescale_matrix.md` were updated together, and `verify_professor_export.py` gained assertions that figure and table numbers ascend in order of appearance and that each image filename matches its number, so the class of error cannot recur silently.
- **CH-142 ★ — a stale cross-reference was baked into a rendered figure.** Figure 4's in-image footnote read "G1–G4 name the missing interval (Table 8)" — v0.27b numbering for what is now Table 4. Because it lived in the image rather than the caption, no text search of the manuscript would have found it; it survived the whole v0.28 journalization. Corrected and re-rendered, and all four figure PDFs were then text-searched for embedded cross-references. No other exists.
- **CH-143 ★ — §4.2 read as a self-contradiction.** It reported <0.1 ms per element and <10 ms per tile [4], then said quantified values across reviewed electronic mechanisms run from ≈15 ms to tens of seconds with the semiconductor end carrying no number. Both statements were true of different quantity types, and the text did not say so. Rewritten to name the types the register already separates: [4] reports **controller write** times (Supplementary Table S2), [5] reports **material transitions**. No number changed and **no semiconductor transition time is inferred** — the text states that none was measured, that nothing supports an inference that such transitions are slow, and that the two values do not belong on one scale. Two downstream instances of the same conflation ("fast electronic phase reconfiguration … has been measured", in §4.6 and the conclusion) now read "timed at the controller".
- **CH-144 — an unsupported cost claim.** The conclusion read "The most valuable next experimental step is also the cheapest" and §6.3 ranked three experiments "by what it would cost to close them". No cost study was performed. Both replaced with hardware-prerequisite wording, with the ordering explicitly labelled a judgement rather than a costing. The word *cheap* no longer occurs in the manuscript.
- **CH-145 ★ — the reference-list header stated something false.** It claimed the entries are "numbered in order of first appearance". The v0.28 restructure moved several first citations and the order now breaks at [19]→[4], [26]→[12] and [18]→[7]. Corrected rather than renumbered: the header now states that the numbering is the project's stable source numbering, that the registers and supplementary tables are keyed to it, and that renumbering into citation order is a submission-time step. The shared-bibliography convention is also now stated on both documents — the manuscript's header records that [33] is cited in the supplement only, and the supplement's header records that its bracketed numbers, tables and figures are the manuscript's and that it carries no separate list. Verified mechanically: 34 entries, 1–34 with no gaps, every citation in both documents resolves, nothing uncited, nothing undefined.
- **CH-146 ★ — `verify_professor_export.py` had not run since v0.23 and could not have.** It opened `Professor_Review_Draft_v0.23.docx`, a filename deleted three versions earlier, and crashed before any DOCX or PDF assertion executed; its structural assertions still demanded 12 sections and 9 tables. **The v0.28 export was therefore shipped unverified.** Filenames now derive from `BASENAME` so they cannot drift; assertions corrected to 7 sections, 5 numbered tables, 6 DOCX tables; new assertions added for ordering, figure filenames and heading/caption styles. Suite now reports **47 passed, 0 failed**. This is the second stale-verifier finding in two passes, after CH-140.
- **CH-147 — the DOCX used hand-formatted Normal paragraphs for headings.** The exporter now defines and applies real **Heading 1/2/3**, **Caption** and a derived **Table Caption** style, each redefined to the body face so Word's blue Calibri defaults never appear. The file now supports a navigation pane and a generated table of contents, and can be retargeted to a journal template by editing five styles. Tables are additionally pinned to 100 % of the text width so Word cannot lay an autofit table beyond the frame.
- **CH-148 — landscape mode leaked past its table in the PDF.** The emitter switched to landscape for a wide table and stayed there until the next heading, leaving Figure 1 and three paragraphs of §3.1 on a page 55 % blank. It now returns to portrait as soon as the table is placed, matching the DOCX emitter. Manuscript 28 → **27 pages**, two landscape pages, each holding one table.
- **CH-149 — two rendering defects found by visual inspection.** Table 2's "Arch." header broke across two lines: the column-width floor was estimated from character counts, which cannot know that a bold 6.2 pt "Arch." is wider than four plain digits, so it now measures real glyph widths from the font metrics and redistributes width from columns with slack. In Figure 2 the "300 ms" and "650 ms" labels touched, because three markers within half a decade were staggered across only two heights; the third now sits below the lane.
- **CH-150 — §5.2 and the conclusion were shortened.** §5.2's reach-of-the-practice paragraph went from 316 to 219 words, the author-network and residual-risk detail reduced to a clause and a pointer to S8.3/S8.4 where it stands in full; both author-disjoint restrained cases remain cited so no reference lost its main-text home. The conclusion went from 844 to **650** words and now carries no numeric timing value at all. Ten rhetorical constructions were replaced across the manuscript. Main-text prose 13 211 → **13 130**, bold spans 34 → **21**, em dashes 5.4 → **4.4** per 1 000 words.

## v1.11 — 22 August 2026 — manuscript v0.28, structural journalization

**A structural and editorial revision, not a scientific one.** No finding was added, removed or changed. C1, C3, C4 and G1–G4 were recomputed from the canonical registers during the move and are unaltered: C1 max Q = 6 (BAI-22, LI-25), max `·T` = 4 (BAI-22 alone), max `·T`+`·(T)` = 6 (LI-25 alone); C3 306 → 262 → 65 → 24 → 20 → 13 read / 7 unread, 4 propagation + 9 no-timing. The full disposition of every subsection, table and figure is in `journalization_v0.28.md`; v0.27b is preserved at `Documentation/archive/manuscript_v0.27b/` and at commit `4e5c89e`.

- **CH-132 ★ — the manuscript read as an internal evidence-audit dossier rather than a review article.** Twelve sections and fifty subsections re-proved the same eight findings in up to eight places each: `16.76` appeared in eight of thirteen section files, `300 ms` in six, the empty-intersection finding in seven, and the three closing experiments in three. Restructured into **seven sections and sixteen subsections**, each finding established once with at most one callback. Main text **22 668 → 15 480 words** (prose 18 713 → **13 211**, −29.4 %), rendered manuscript **41 → 27 pages**.
- **CH-133 — the audit machinery outweighed the argument.** The two-axis coding scheme, the seven-level evidence ladder, the per-source stage matrix (old Table 3), the states-tracked table (old Table 2), the propagation inventory (old Table 6) and the full ~30-field reporting checklist (old Table 8) were moved to the supplement as new sections **S6, S7, S8** and an expanded **S4**. Supplement **4 551 → 7 729 words**, 8 → 15 pages. **No row of any moved table was dropped.** Main tables 8 → 5; main figures unchanged at 4, none regenerated.
- **CH-134 — three units were deleted as redundant rather than moved.** Old §4.5 was a section summary restating the section above it; old §7.2 re-proved §6.4 with the same numbers (the substitution mechanism it illustrated was preserved as one sentence in §4.3); old §9.1 restated §6.7. Nothing else was deleted.
- **CH-135 — report-like formatting.** Body-text bold spans **241 → 34 (−86 %)**; em dashes **10.5 → 5.4 per 1 000 words**. Removed "the single most consequential fact in the taxonomy" and "the single most useful set of numbers in the corpus", the three surviving "an earlier draft of this review" style meta-comments having gone at v0.26.
- **CH-136 — four unbounded negative claims.** "What no source measures is the intersection", "the four measurements … have not been made", "the combination has not been built and timed" and "they have not been measured" were bounded to the reviewed set. Two more were bounded in the professor brief, whose traceability paragraph now also carries its 13-of-20 access bound explicitly. A full audit of every occurrence of *no source / no platform / has not / none / never / first / only* across the new manuscript found no other unbounded case; no occurrence of "first" is a novelty claim.
- **CH-137 — five references would have been left cited only in the supplement.** [27] and [28] lost their only main-text citation when old §3.7's figure-cell listing was compressed; [31] and [32] lost theirs when the propagation inventory moved to S8. [27]/[28] were restored to §3.1 as part of the list of platforms plotted in Figure 1, and [31]/[32] to §5.2's identification of the four propagating studies. **[33] remains cited only in the supplement** (the S5 register), which shares the manuscript's numbering; recorded rather than fixed, since it is cited for its published-record authorship alone.
- **CH-138 — the combined supplement was hand-assembled and could drift from its parts.** New `tools/build_supplement_combined.py` concatenates S1–S8 in order and fails if a part is missing. `build_professor_export.py`'s section ORDER was updated to the seven new files, and its table column-width floor raised from +1.5 to +2.5 characters, which was breaking the short "Class" header of Table 1 across two lines.
- **CH-140 ★ — `validate_public_snapshot.py` still asserted the pre-v0.24 C1 values, and had not been run since.** It asserted `max_fully_delimited_T == 5` and that the `·T`+`·(T)` maximum was reached by both BAI-22 and LI-25. Both were superseded at v0.24, when the timing taxonomy was rebuilt and LI-25's S2 RMSD was reclassified as an accuracy: the current values are **4, BAI-22 alone** and **6, LI-25 alone**. Run against the current tree the suite reported **31 passed, 2 failed**, so the GitHub Actions workflow would have failed on the first push after v0.24 — it never ran, because v0.24 through v0.27b were all held unpushed. Assertions corrected and the reason recorded in the file; the suite now reports **33 passed, 0 failed**. The same stale maximum was found in two active supporting records (`draft_status.md`, `manuscript_argument_map.md`) and corrected, and `check_s11_consistency.py` gained two patterns for it, verified by reintroducing the pre-fix line and confirming the checker exits non-zero.
- **CH-139 — supplement cross-references pointed at deleted sections.** S1 named "Table 4" for the main register (now Table 2), S3 pointed at "Section 8" and "§7.2", S4 at "Table 8" and "§9.2", and the S5 generator at "Section 8". All repointed; S4 retitled "The complete reporting checklist" to state its relation to the compressed main Table 5.

## v1.10 — 19 August 2026 — manuscript v0.23, public-artifact reproducibility freeze

**No scientific result changed.** C1 and C3 were independently recomputed from the published v0.22 files and survive unaltered: C1 max Q = 6 (BAI-22, LI-25), max fully delimited `·T` = 5 (BAI-22 alone), max `·T`+`·(T)` = 6 (both); C3 306 → 262 → 65 → 24 records → 20 studies → 13 read / 7 unread, with 4 propagation + 9 no-timing + 7 unread. This release repairs the artifact, not the argument.

- **CH-122 ★ — the public toolchain did not run on a case-sensitive filesystem.** Ten shipped scripts hardcoded `Documentation/` while the mirror's directory is `documentation/`. On macOS this is invisible; on a Linux checkout — where an independent reviewer actually runs it — `c1_stage_counts.py`, `s11_counts.py` and `check_s11_consistency.py` all failed immediately with `FileNotFoundError`. **The v0.22 claim that the mirror had been verified as independently runnable was therefore true only on the author's filesystem.** Reproduced on a case-sensitive APFS volume, then fixed: new `tools/_paths.py` provides one shared, case-correct path definition that resolves in both trees, and every shipped tool imports it. Verified by re-running the full suite from a case-sensitive volume.
- **CH-123 ★ — `build_forward_citation_csv.py` still contained pre-v0.22 science.** It read a file deleted in v0.22, and its embedded decision dictionary still carried the retired `F-ANJ-C` / `F-ANJ-J` split and the withdrawn `counterexample` role. Running it would have regenerated an obsolete C3 dataset. Rebuilt so that it contains **no scientific content at all**: the human-coded layer moved to `documentation/s11_manual_decisions.csv` (65 rows, one per stage-1 survivor) and the script now only joins that to the deduplicated works. `--check` verifies the rebuild is **field-identical** to the committed decision table, and the script exits non-zero if a retired family or role ever reappears. Bibliographic data likewise moved to `documentation/s11_study_citations.csv`. **No tool in the package now holds scientific state.**
- **CH-124 — `adaptation_chain_matrix.md` §3.1 still contradicted the corrected manuscript.** Its heading claimed "the maximum is five timed stages, and two platforms reach it", and its table credited LI-25 with timing S1, S2, S4, S5, S6 — although S2 and S5 are `·(T)`. Rewritten to give the three counts explicitly and per platform: BAI-22 `·T` = 5, `·(T)` = 1, total 6; LI-25 `·T` = 4 (S1, S4, S6, S10), `·(T)` = 2 (S2, S5), total 6. Now matches §5.3 exactly, with the wording constraints made binding.
- **CH-125 — the screening expression has 46 alternatives, not 47.** Independent parsing of the pushed script gives `len(WIRELESS_TERMS) == 46`; the number 47 was hand-maintained prose that no artefact checked. **The expression was not changed** — it is the one that produced the recorded stage-1 count, and altering it to preserve a number would have changed the science for cosmetic reasons. Stage-1 re-verified at **65**, unchanged. The count is now reported from `len(WIRELESS_TERMS)` in `s11_counts.py` and the generated register, and the checker fails if any active document disagrees.
- **CH-126 — `literature_search_log.md` still described a deleted file as the raw retrieval.** Its reproducibility-artefact list named `forward_citation_stage1_raw.csv` and called it "the unmodified stage-1 retrieval". Replaced with a table of the four current data files and the current tools, at their real lowercase public paths, with the correction noted.
- **CH-127 — active supporting records still described v0.21 as current.** `draft_status.md` was headed v0.22 but retained "What v0.21 is", the v0.21 word count (29 049 / 23 472), "33 entries" against an actual 34, and a readiness statement pointing at v0.21. Word count recomputed by the standing method: **30 343 total / 24 738 prose / 5 605 table**.
- **CH-128 — `unresolved_questions.md` U8 asserted something now false.** It still read "No co-authorship analysis was performed" although v0.22 performs one and C3 rests on it. Rewritten with the method, the result (14 / 4 / 2 of 20 studies), and what remains limited — authorship is not intellectual independence, and seven studies remain unread.
- **CH-129 — the consistency checker did not perform the scan it claimed.** It built `DOC / "Documentation"` and `DOC / "manuscript"`, so on a case-sensitive checkout it scanned nothing while reporting success. Rebuilt to scan the manuscript, documentation, audits, the generated review files, `tools/README.md` and the sync templates — 42 documents and 13 tools — with new checks for wrong-case paths in shipped tools, the retired filename, the retired C1 phrasing, the false co-authorship sentence, superseded word counts, and any alternative count disagreeing with `len(WIRELESS_TERMS)`.
- **CH-130 — new `tools/validate_public_snapshot.py`, plus CI.** 33 assertions covering exact directory casing, offline execution of every derivation, the expected C1 and C3 values, absence of retired filenames and scientific state, link integrity and manifest uniqueness. A GitHub Actions workflow runs it on `ubuntu-latest` on every push to the public mirror, so the case-sensitivity failure cannot recur silently.
- **CH-131 — the review manifest listed one path twice.** `documentation/s11_study_register.md` was matched both by the documentation glob and by its explicit whitelist entry. The whitelist now de-duplicates by destination.

## v1.9 — 19 August 2026 — manuscript v0.22, C1/C3 integrity pass

Repairs three defects found by independent inspection of the **published v0.21 review mirror**, plus the supporting-documentation drift they exposed. No section was restructured, no claim broadened, no source added.

- **CH-111 — §2.4 described a method the manuscript no longer used.** The extraction protocol still coded each stage as "demonstrated, measured, simulated, assumed, absent or unclear", a one-axis scheme contradicting the two-axis coding introduced in §5 at v0.21. §2.4 now defines Axis A (stage evidence status) and Axis B (timing status) in full, and states Axis B as **three-valued**: `·T` fully delimited, `·(T)` partially delimited, unmarked untimed. Table 3's sub-legend and Figure 2's legend were aligned to the same wording.
- **CH-112 — C1's headline count followed from no consistent rule.** "No reviewed source times more than five of the ten stages; two platforms reach five" counted LI-25's two `·(T)` cells as timed while discarding its `·T` at S10, and counted BAI-22's five `·T` cells while discarding its `·(T)` at S8. Recount from the matrix, now performed by `tools/c1_stage_counts.py`: quantitatively evidenced stages max **6** (LI-25, BAI-22); fully delimited `·T` max **5**, reached by **BAI-22 alone**; any timing information max **6**, reached by both. C1, the abstract, §5.3, §9.4, §12 and the argument map restated. The disjointness finding survives and is sharper: the two platforms' timed sets share S1, S2, S4, S5 and differ exactly at S6/S10 against S7/S8.
- **CH-113 — the S11 dataset violated the manuscript's own independence rule.** §2.4 item 5 states that a conference paper and its journal extension are one lineage, and the reference list labels [32] the journal extension of [2] — yet the dataset counted them as two study families. Full lineage audit of all 24 stage-2 records against OpenAlex author lists, venues, types and dates: one merge (**F-ANJ-C + F-ANJ-J → F-ANJ-DL**), five further candidate pairs examined and deliberately **not** merged, with reasons recorded in `tools/repair_study_families.py`. **21 → 20 distinct studies; 14 → 13 read.**
- **CH-114 — the "eight papers attach no timing value" count was not reconstructable.** Prose counted eight *including* the Table 7 counterexamples; the CSV counted eight *excluding* them. Two different sets of equal size. Counterexamples N1–N3 are now recorded as members of the no-timing-value set rather than a separate class, and the set is **9 studies / 11 records**, listed by identifier in the generated `s11_study_register.md`. The 13 studies read now partition exactly: **4 propagate + 9 attach no timing value**.
- **CH-115 — the author-network denominator was wrong.** "17 of the 20 identified FIM system papers share an author with that network" was incorrect and rested on an *unresolved* category for the seven unread studies. Authorship is verifiable from published-record metadata even where full text is not: all 20 studies now resolve to **14 sharing a lineage author, 4 disjoint from the lineage, 2 disjoint from the lineage and all three seeds**. This sharpens C3's bound — of the seven unread studies, exactly **one** is author-disjoint from both, so the residual risk to the independence finding is that single study rather than seven unknowns.
- **CH-116 — the reproducibility scripts the manuscript claimed were published were absent from the public mirror.** §2.3 states the query and expression are "published as a runnable script alongside this manuscript"; the v0.21 mirror had no `tools/` directory. The sync whitelist now carries the S11 scripts, the C1 counter, the consistency checker and a `tools/README.md`.
- **CH-117 — `forward_citation_stage1_raw.csv` was not what its name said.** It held 262 deduplicated work-level rows *with decision columns*, not the 306-record pre-deduplication retrieval. Renamed **`forward_citation_works_deduplicated.csv`**, and the genuine artefact is now published as **`forward_citation_records_raw.csv`** (306 rows, one per seed-work pair, no screening columns), retrieved by `tools/retrieve_306_records.py`. Re-execution reproduced 306 records / 262 works exactly (per-seed 160 / 102 / 44).
- **CH-118 — residual unbounded absence claims.** "No citing paper anywhere in the retrieved set derives, explains or sources the tabulated 10 ms value" cannot be known from seven unread studies. Bounded to "in any of the full texts inspected" in §8.5, `literature_search_log.md` and `manuscript_argument_map.md`.
- **CH-119 — the conclusion's preprint warning overstated what remains open.** Replaced with the actual position: every load-bearing quantity has been checked against its version of record; **one** version check remains open (ALE-26) and is peripheral, cited once for class membership with no quantity drawn from it.
- **CH-120 — stale S11 counts across the active supporting documentation.** `literature_search_log.md`, `independence_audit.md`, `absence_claims.md`, `novelty_boundary.md`, `unresolved_questions.md`, `draft_status.md` and `manuscript_argument_map.md` all carried superseded denominators (64 stage-1, 20/13, 21/14, 17-of-20). All corrected, and **`tools/check_s11_consistency.py`** now fails the build on any recurrence; `s11_study_register.md` and the mirror's generated files derive their counts from `tools/s11_counts.py` instead of prose.
- **CH-121 — generated review-repository files carried hardcoded scientific prose.** `REVIEW_INDEX.md` stated 33 references against an actual 34, quoted superseded C1 and C3 wording, and linked to `review/SOURCE_VERSION_MAP.md` when the file is at the repository root. Reference count and all S11/C1 figures are now generated; the link is fixed and a relative-link checker runs over the whole mirror.

# Changelog

All entries are dated and attributed. Nothing in this documentation set silently replaces a prior project record; corrections are stated with their evidence.

---

## v1.8 — 18 August 2026 — reviewer-blocker revision

Scope: **targeted repair of the methodological and evidentiary problems identified in an independent review of v0.20.** Not a rewrite. Manuscript draft v0.20 → **v0.21**; documentation v1.7 → **v1.8**. Entries CH-100 … CH-110.

> **What changed on the evidence, and what did not.** Three counts changed and four corrections were made, all of them because a check was performed that had not been performed before. No claim was strengthened. C3 remains at **Level A**. The central findings — five of ten stages, four propagation instances in one co-authorship network, zero author-disjoint groups reproducing the practice — all survive, two of them now stated more precisely and all of them more narrowly bounded.

### The adaptation-chain recoding

**CH-100 ★ — The stage coding was internally inconsistent and has been rebuilt on two axes.** `adaptation_chain_matrix.md` v1.0 defined `M` as "measured (a number with defined start/end events)", but applied it to cells recording quantitative **radio-frequency** measurements containing no duration: LU-26's patterns at fixed curvature, NEU-24's patterns, AKR-26's patterns and power, GAL-22's ≈80 mW at 1 m. Those are real measurements and none is a timing. The consequence was a genuine ambiguity in a load-bearing count: *"no source measures more than five of the ten stages"* is **true under a timing reading and false under a quantitative reading**, where the maximum is six.

The fix separates **axis A — stage evidence status** (`Q` quantitatively measured / `D` demonstrated / `S` simulated / `A` assumed / `✗` absent / `n/a` / `?`) from **axis B — timing status** (`·T` duration measured with identifiable start and end events / `·(T)` duration reported but incompletely defined or not resolved from an adjacent stage / unmarked, untimed). Every S1–S10 cell was recoded. `adaptation_chain_matrix.md` → **v2.0**.

**CH-101 ★ — The recount changed what C1 says, and made it stronger.** The number five survives *as a timing count*, and the verb is now always **times**, never *measures*. Two platforms reach five and are **disjoint in the decisive respect**: LI-25 times S1–S2 and S4–S6 on an aperture that radiates but does not command its shape; BAI-22 times S1–S2, S4–S5 and S7 on a surface that commands its shape but has no RF layer. The two sets differ by exactly one stage — S6 against S7 — which is the difference between compensating a shape and commanding one. New facts the recoding surfaced: **S9 is timed by no reviewed source at all**, and **stabilised RF operation is quantitatively measured on five platforms and timed on one, which holds its geometry static**. Figure 2 regenerated with the two-axis legend.

**CH-102 — One stage placement was corrected.** YAN-25's measured wall-clock runtime was recorded under S4 (optimisation). The timed algorithm is CMFV-SBL, which estimates the channel, so it belongs to **S3**. No count changes; YAN-25 times exactly one stage either way.

### The forward citation search

**CH-103 ★ — The screening regex contained an ellipsis and was therefore not reproducible. It has been written out, re-executed, and three counts changed.** The recorded query ended `…`, which is not a query. It is now an explicit 47-alternative expression published in `tools/reproduce_forward_citation_search.py`, together with the exact seeds, the exact API calls, the deduplication rule and the full-text extraction expression. Re-execution against the live OpenAlex API **reproduced retrieval and deduplication exactly** (306 records → 262 unique works) and **changed the screening counts**: stage-1 64 → **65**, system studies 20 → **21**, full texts read 13 → **14**. Unread remains **7**. Propagation instances (5) and papers (4) are **unchanged**, as is the zero author-disjoint reproduction count.

**CH-104 ★ — The one work the ellipsis had hidden is a substantive gain.** Writing out the regex added `beam-?steering`, which captured **Bansal, Hewson, Santer & Whittow, "Optimal Morphing Metasurface Lens for Next Generation RF Sensing and Communications", EuCAP 2024** (DOI 10.23919/EuCAP60739.2024.10501383). It was retrieved in full from the authors' open figshare deposit and read. It proposes a *mechanically morphing* metasurface lens for RF beam steering, cites BAI-22 as its reference [15] — and **attaches no timing value of any kind**; a full-text scan for the complete timing expression returns zero hits. Its authors share no author with the FIM lineage or with any seed. It is therefore a **second fully author-disjoint counterexample**, added as manuscript reference **[34]** and Table 7 row **N3**. It does not move C3 off Level A: that would require an author-disjoint group *reproducing* the substitution, and this is the opposite.

**CH-105 ★ — A claim was found that rested on a source we had not read, and has been withdrawn.** v0.20 §8.5 and Table 7 row N1 counted **HUA-25** among papers that "attach no timing value at all". That is a statement about a document's contents, and its full text is paywalled and was never read — only its Crossref author metadata. **Removed from the counterexample count.** It is now cited solely for its verified authorship (the Taisong Pan overlap with LI-25, the CH-97 correction), and §8.5 states explicitly that whether it attaches a timing value is unknown to us. **Standing rule adopted: no source that has not been read in full may support a statement about what that source does not contain.**

**CH-106 — C3 is now conditioned on its observation window wherever it appears.** The abstract, Introduction C3, §8.5, §9.5 and §11.5 all now state that the finding holds **among the 14 of 21 candidate studies whose full texts were read**, that an abstract-level scan establishes nothing because propagation occurs only in body text, and that **an author-disjoint instance among the seven unread studies cannot be excluded**. "All four share authors" is replaced by **"one connected co-authorship network"**, which is what was actually verified.

**CH-107 — Reproducibility artefacts published.** `tools/reproduce_forward_citation_search.py`, `tools/build_forward_citation_csv.py`, `Documentation/forward_citation_stage1_raw.csv` (unmodified retrieval) and `Documentation/forward_citation_search_results.csv` — **one row per each of the 262 unique works**, carrying stage-1 result, stage-2 decision and reason, full-text status and source, timing hit, propagation case, author-network status, final role, and the `version_family` that collapses records into studies. Retrieval and stage-1 are exactly reproducible; stages 2 and 3 are auditable per work but not re-derivable, and the log says so.

### Bounded language

**CH-108 — Every unbounded absence and priority claim in active text was located and bounded.** A literal sweep for *anywhere / nobody / no one / never / first / only / none / all / the literature / the field* was run over the manuscript, the figure script, all captions and tables, and active documentation; every hit was inspected individually. Corrected: Figure 3's two `NOT MEASURED ANYWHERE` boxes → **`NO MEASUREMENT FOUND IN REVIEWED SET`**, and its lane renamed to **"commanded mechanical actuation of a radiating aperture"**; §9.3 "nobody has built the object…" → "no platform in the reviewed evidence base is the object…"; §9.5 "quantities nobody has measured" → "quantities for which no measurement was found in the reviewed set"; §9.4 and §12 "would produce the first mechanical characterisation…" → "would directly fill G1 and G2 in the reviewed evidence base"; §11.3 "the first quantitative constraint" → "would directly fill G3"; §1.2 "nobody has measured how long the surface takes" → "no source in the reviewed set has measured…"; §5.6 "the only demonstrated shape-control loop in this literature" → "the one demonstrated shape-control loop in the reviewed set"; §7.5 "never following a commanded morph" → "among the sources examined here, not following a commanded morph"; §7.3 "timed anywhere" → "timed in the reviewed set"; Table 9 "never for shape achievement" → "not reported … anywhere in the reviewed set"; and the same two boxes in `timescale_matrix.md`'s ASCII summary. Legitimate bounded uses — "no value at or near 10 ms anywhere in **its complete package**", "no channel estimator anywhere in **the system**" — were left alone.

### Corrections to the record

**CH-109 ★ — Two more bibliographic dates were wrong in the same way, and a third pattern was found.** v1.7 cited AKR-26 as *Adv. Electron. Mater.* **vol. 12, no. 1, Dec. 2025** — internally inconsistent, because Crossref gives `published-online` 12 Dec 2025 and `published-print` **January 2026**; volume 12 issue 1 *is* the 2026 issue. Corrected to **12(1), art. e00674, Jan. 2026**, with the online-first date noted separately. **The identical defect was then found in CHE-26** (online 28 Sep 2025; issue **Feb 2026**) and corrected. Issue dates and article numbers were also regularised for SAI-22 (Nov 2022, art. 2203747), LU-25 (Mar 2025) and NEU-24 (art. 70). Propagated to `99_references.md`, `source_inventory.md` §3b and the public-mirror source-version map.

**CH-110 — `source_inventory.md` M-C was stale and self-contradictory, and has been rebuilt.** It listed RAN-25, ANJ-25, YAN-25, MA-26, XU-22 and ALE-26 under "Journal versions not inspected", while §1 of the same file recorded that three of those versions of record had been obtained, archived and inspected and a fourth read from an open repository. Replaced by a per-source table stating separately, for each: whether a version of record exists, whether it was obtained, whether it was inspected, whether quantitative claims were verified against it, which version the bibliography cites, and what remains unresolved. **Exactly one genuinely open item survives (ALE-26), and it is peripheral.**

### Other corrections in this pass

- **§3 retitled** "Seven architecture classes relevant to FIM evidence transfer", with the classes split into **direct** (A1–A4) and **comparator / evidence-donor** (A5–A7), because A5, A6 and A7 authors do not generally use the FIM label and the previous title overstated the nomenclature.
- **"The FIM concept is blocked by none of them" withdrawn** from §6.7 and §12. Separate demonstrations of constituent capabilities do not establish that those capabilities stop constraining the design once integrated. Replaced with: *"None of these constituent capabilities is absent from the reviewed evidence base when considered separately; what remains unvalidated is their integration and its timing on one architecture."*
- **Reporting framework made advisory.** "Mandatory" and "all five required" replaced by "core fields", "recommended minimum reporting set" and "should be reported when applicable", consistent with §10.4's own statement that we have no standing to propose a standard. No field was removed and no numerical threshold was added.
- **The uncertainty statement was too categorical and is now correct.** "No interval quoted in this manuscript carries an uncertainty" is disproved by the manuscript's own Table 5 row 28 (0.35 ± 0.15 s). Now scoped to **headline response, morphing and adaptation intervals**, with the exception named — which strengthens the point, since it shows those authors reported dispersion where they had it. "No amount of further searching would repair it" replaced by the accurate reason: additional literature cannot retroactively supply uncertainty for values already published without it; only raw data, reanalysis, repeated measurement or new experiments could.
- **Peer-review-process language removed from the manuscript.** "This manuscript has not been reviewed by a domain expert" is a statement about our workflow, not a scientific limitation, and implied that peer review must precede submission. Replaced in §11.4 and §12 with **"Extraction and classification were performed by a single reviewer and were not independently duplicated or adjudicated"**, naming the specific judgements most exposed. Retained in `draft_status.md` as an internal project gate.
- **"24 independent research contributions" → "24 distinct research contributions"** in §2.2 and `source_inventory.md`, with a pointer to the independence groups. Independence was never established for all 24 and several share authors.
- **Conclusion's fast-fading statement tightened** to avoid reintroducing the universal-coherence-time logic the manuscript rejects elsewhere: instantaneous fading "supplies the fastest validity timescale among the states considered — but only for operations conditioned on instantaneous CSI", followed explicitly by the point that not every control variable inherits that timescale.
- **Reflective-stack language scoped.** Statements listing meta-atom metallisation, dielectric substrate, bias network and ground plane are now attached to **grounded reflective apertures of the kind the reviewed flexible platforms use**, with transmissive and active architectures explicitly excluded from the claim.
- **Figures 2 and 3 regenerated** from the committed script. Figure 3 keeps closed-loop search, open-loop replay and static-geometry stabilised RF on separate lanes, and remains explicitly not a latency budget.

---

## v1.7 — 18 August 2026 — publication-oriented authoring pass

Scope: **turn the frozen evidence base into a journal-review manuscript.** Full rewrite and restructuring, all nine tables and all four figures produced, numbered IEEE-style bibliography built from verified metadata, internal evidence keys removed from the reader-facing text. Manuscript draft v0.14 → **v0.20**; documentation v1.6 → **v1.7**. Entries CH-80 … CH-99.

> **Freeze status.** The scientific evidence base was **not revised** in this pass. Claims moved between sections and prose was rewritten; no value, verdict, classification or absence claim changed on the evidence. The two exceptions are CH-96 and CH-97, which are factual errors found while completing the bibliography and corrected rather than carried forward.

### Structure

**CH-80 — Ten sections became twelve, and the reasons are recorded rather than asserted.** Scope and method left the Introduction and became **§2**, because a structured critical review is judged on its method and the method should be locatable. The 4 300-word §6 was split: synthesis stayed as **§7**, and **the traceability audit became §8**, a section of its own with a table of its own — it is the strongest contribution and was previously the fifth subsection of the longest section. Hardware evidence absorbed the timing register as **§6**. Electronic and mechanical hardware remain in one section, for the v1.1 reason: separating them would hide the range-overlap finding. Full mapping and a **v0.14 → v0.20 section-number crosswalk** are in `manuscript_argument_map.md` **v2.0**; a pointer banner was added to every documentation file that cites section numbers, so no older file is silently stale.

**CH-81 — File-level record.** `manuscript/` now holds `00_front_matter.md` and `01`–`12`, plus `99_references.md` and `figures/`. The nine v0.14 section files were removed; the git baseline `69092a2` holds them, and the argument-map crosswalk says where each argument went.

### Tables and figures

**CH-82 ★ — All nine tables are produced.** Table 1 architecture taxonomy · Table 2 states the system must track · Table 3 adaptation-chain coverage · Table 4 timing-quantity taxonomy · **Table 5 the timing register** · Table 6 stage-by-stage answer · **Table 7 timing-value propagation** · Table 8 assumptions vs evidence vs missing measurement · Table 9 reporting framework.

**CH-83 — The count changed from seven to nine, deliberately.** Two objects that were prose needed tabulating: the **timing-quantity taxonomy** (Table 4), without which the register cannot be read under the comparability rule, and the **propagation cases** (Table 7), which are the evidence for C3 and were buried in a paragraph. Two v0.14 placeholders — "system assumptions vs physical evidence" and "validation gaps" — were **merged into Table 8**, because separately they repeated each other's rows.

**CH-84 ★ — All four figures are produced**, as vector PDF and 400-dpi PNG, from `manuscript/figures/make_figures.py`, which is committed and re-runnable. Figure 1 architecture–evidence map with the empty cell · Figure 2 adaptation-chain coverage · **Figure 3 timing landscape** · Figure 4 assumption–evidence links.

**CH-85 — Figure 3 obeys the standing constraint.** Closed-loop **search** (≈2.5 min) and open-loop **replay** (10 fps) are on **separate labelled lanes**, and every lane is separate because the quantities are not commensurable. The caption states that the axis is not a latency budget and that no value may be pooled with, ranked against or substituted for a value in another lane. Two lanes carry no value at all. Requirement from `draft_status.md` v1.6 item 5, discharged.

**CH-86 — Figure 1 keeps A1 off the map.** Both axes are properties of an experiment; a modelling paper performs none. A1 is drawn as an annotation naming the empty cell — the modelled target operating point — never as a plotted platform.

### Citations and bibliography

**CH-87 ★ — The manuscript now uses a numbered IEEE-style citation system**, ordered by first appearance, with **33 references**. Internal keys (`RAN-25`, `E-03`, `T-LI-04`) survive in the documentation and appear nowhere in the reader-facing text. Table row labels use "Author *et al.* [n]".

**CH-88 — Author lists were read from the held PDFs, not reconstructed.** The three rasterised sources (PEP-26, LU-25, CHE-26) were rendered as page images and their title pages read. No bibliographic field in `99_references.md` is invented.

**CH-89 — Every DOI was resolved against the publisher-deposited Crossref record**, and the arXiv metadata endpoint was queried for every preprint. Refinements recorded in `source_inventory.md` **§3b**: AKR-26 is *Adv. Electron. Mater.* **12**(1), issued Dec 2025; CHE-26 is *Laser Photon. Rev.* **20**(3), issued Sep 2025; TIS-25 is **27**(5):2895–2936, no longer "Early Access"; page ranges added for LU-26 and LIH-19. The standing rule is reaffirmed: **the bibliography uses the version-of-record year, and the internal keys are never a citation.**

**CH-90 — External sources verified for the bibliography.** Full author lists and publication data obtained for HU-26, XIA-26, ANJ-25J, ANJ-MIMO, KUM-25, HUA-25, LFIM-26 and FAS-26 (see `source_inventory.md` §3b).

**CH-91 ‼ — LFIM-26 shares three authors with KUM-25.** They are two publications from one line of work. No manuscript claim depends on their being independent — both are cited only for the *existence* of published statistical-CSI FIM optimisation — but they must never be presented as two confirmations.

### Manuscript content

**CH-92 — The Introduction was rewritten to the required progression** and no longer front-loads numbers: physical degree of freedom → why high mobility makes it urgent → why one reported response time cannot settle it → what the adjacent reviews cover → the gap → the question → the instruments → the contributions. The **Doppler ≠ correlation interval ≠ update deadline** distinction is stated in §1.2 and restated at full precision in §4.1.

**CH-93 — §4 was rebuilt around state-specific reasoning.** Table 2 separates instantaneous fading, channel statistics, angles and path geometry, target kinematics, blockage, surface geometry and calibration state, records the evidence available for each, and **contains no update deadline in any column**. RAN-25's version-of-record Remark is presented with **both halves**: the millisecond assertion, and the explicit alternative of morphing once over several coherence intervals — which is why the two-timescale reading is a premise the literature supplies rather than one this review imposes.

**CH-94 — §8.6 states the mechanism rather than the fault.** Informal terms — "response time", "morphing time", "morphing period", "switching speed" — name a duration without naming its object, start event or end criterion, so nothing on the page distinguishes an element from a surface or a replay from a search. The section disclaims bad faith explicitly and points to the remedy in §10.3 item 4.

**CH-95 — The self-correction is retained and rewritten.** Per the U12b decision, the disclosure of our own retracted convergence derivation stays in the body, at **§7.2** (full, as a blockquote beside the values that invite the error) and **§11.4** (one paragraph). It is written as an argument, not as an incident report.

### Corrections found during the pass

**CH-96 ‼ — The DOI held for BUD-22 was wrong, and is corrected.** `source_inventory.md` recorded `10.1109/MAP.2022.3169344`; **that DOI resolves to a different paper** (Bodehou *et al.*, *IEEE Antennas Propag. Mag.* **64**(4):24–36). The correct record, verified by Crossref title query: J. Budhu, L. Szymanski and A. Grbic, *IEEE J. Microw.* **2**(3):401–418, Jul. 2022, **`10.1109/JMW.2022.3181719`**. No claim in the manuscript depended on the DOI, so nothing else changes — but the entry would have produced a wrong citation in the submitted bibliography, and it was caught only because every DOI was resolved rather than trusted.

**CH-97 ‼ — One counterexample's independence status was overstated, and is corrected.** `evidence_matrix.md` **E-FP-C1** recorded "no author overlap" for **two** counterexamples. That is correct for **Kumar *et al.*** (*IEEE WCL* **15**:1150–1154, 2026), which shares no author with the FIM system-paper lineage *or* with any of the three primary hardware sources. It is **too strong for Huang *et al.*** (*IEEE JSAC* **44**:1577–1588, 2026), which is disjoint from the system-paper lineage — the set the S11 independence test measured — but **shares an author, Taisong Pan, with LI-25**. The row now records this, and §8.5 and Table 7 row N1 state which counterexample is fully disjoint and which is not. **The C3 classification is unaffected**: it turns on whether any author-disjoint group *reproduces the practice*, and the count there remains zero.

**CH-98 — Three wording defects introduced during drafting were caught in the mandatory self-review and fixed.** (i) §1.1 called the FIM system papers "independent formulations" — An and/or Yuen appear in all of them, and §8 tests independence by author-set intersection; "independent" is withdrawn. (ii) "the adaptation chain has never been measured end to end" appeared in the abstract, §5.4 and §12 — an unqualified absolute, forbidden by `absence_claims.md` §2; all three are now scoped to the reviewed set. (iii) §1.6 said the search found the substitution "in four papers rather than two" — a fact about our own earlier draft, meaningless to a reader, and exactly the internal-audit residue this pass was meant to remove.

**CH-99 — The evidence ladder is numbered explicitly in §2.4**, because Table 8 uses L2/L4/L5/L6/L7 as labels and the prose form left them undefined for a reader.

---

## v1.6 — 18 August 2026 — evidence closure before publication-oriented authoring

Scope: **close the research blockers capable of materially changing the paper's contribution.** Forward citation search (U3), remaining source-version checks (ANJ-25, ALE-26), U12b decided, git baseline established. No figures, no tables, no stylistic rewriting, no shortening, no CST material. Manuscript draft v0.13 → **v0.14**; documentation v1.5 → **v1.6**. Entries CH-70 … CH-77.

**CH-70 ★★ — U3 is closed: the forward citation search was performed, and it is reproducible.** Method, queries and screening counts are in `literature_search_log.md` **S11**. Exhaustive cited-by retrieval from the OpenAlex citation graph for BAI-22 (`W4296552404`, 160 citing), NI-22 (`W4297022396`, 102) and LI-25 (`W4409148742`, 44) → 306 records → **262 unique works** → **64** wireless/communications-relevant → **20** distinct flexible-metasurface system papers → **13 full texts obtained** (arXiv author versions plus one open institutional-repository copy). **7 were paywalled and not read, so every count is a lower bound.** No paywalled full text was retrieved at any point.

**CH-71 ★ — The audited Table I was located, retrieved and verified verbatim.** Hu, **An**, Gan, H. Li, Al-Dhahir, Karagiannidis and Nallanathan, *"Weighted Sum-Rate Enhancement for FIM-Assisted Multicell Systems"* (*IEEE TWC* 2026, `10.1109/TWC.2026.3701359`, arXiv 2606.06845). Table I "Key Parameters of Existing FIMs" lists **Morphing Period 30 ms (Ni), 10 ms (Bai, dated 2021), 500 ms (Niu)**. The corroborating figures match BAI-22 exactly (18 mm aperture, 5.5 mm deformation, ratio ≈0.3), confirming the entry derives from that paper. CA-01 and CA-02 are now verified against the citing paper's own text rather than through a third-party summary.

**CH-72 ‼ — Two escalations were found that the manuscript did not previously know about.** (i) The same paper states that FIM deformation runs *"on the order of milliseconds (with the fastest reaching **1 ms**)"* — a figure **faster than every entry in its own Table I**, whose minimum is 10 ms, and one that appears in **no** primary source. (ii) It further states that *"the response time of FIM surface morphing reaches 10 ms according to Table I and aligns with typical channel coherence blocks, providing a sufficient time window for joint optimization"* — converting a tabulated value into a system-feasibility argument. Registered as **E-FP-01** and **E-FP-02**.

**CH-73 — Two further propagation instances, one of them a version-stage insertion.** **E-FP-03:** Xiao, J. Wang, Cui, Yang, X. Li, Niyato and **Yuen**, *"Channel Estimation for Flexible Intelligent Metasurfaces"* (*IEEE TWC* 2026, arXiv 2508.00268v4) states that the cited prototypes *"have demonstrated shape-switching times on the order of milliseconds, which are comparable to or faster than the channel coherence time in typical mmWave scenarios"*, citing LI-25, NI-22 and BAI-22 together — including LI-25, whose geometry is **static and externally imposed** throughout the interval it reports. **E-FP-04:** the journal version of ANJ-25's study (*IEEE TWC* **24**(4), 2025) states in Remark 4 that *"it is possible to achieve dynamic surface-shape morphing in just a few milliseconds"*, citing BAI-22; **the held conference version contains no occurrence of "millisecond" or of any `<number> ms` value.** This is the same add-at-journal-stage pattern as RAN-25's Remark (**E-FP-05**).

**CH-74 ★★ — The independence result is negative, and it is the finding that governs C3's wording.** Independence was tested by author-set intersection, not assumed. **No author-disjoint publication group reproduces the practice.** Jiancheng An appears in three of the four instances, Chau Yuen in three; **17 of the 20** identified FIM system papers share an author with the corpus FIM lineage. The case base widened from two papers to four; the independence basis did not widen at all. **C3 is therefore classified LEVEL A** — see `novelty_boundary.md` N3, where the three levels were fixed before the search and the evidence read against them afterwards. Level B was not met and the stronger wording is not taken.

**CH-75 — The counterexamples are recorded, because an audit that reports only confirmations is worthless.** Seven retrieved papers cite the same primary sources and attach **no timing value at all** (**E-FP-C1**), including two — Kumar *et al.*, *IEEE WCL* 2025, and Huang *et al.*, *IEEE JSAC* 2025 — with **no author overlap** with the FIM lineage. Separately (**E-FP-C2**), An, Han, Niyato and Yuen, *IEEE TCOM* 2025, names response time explicitly as an unresolved practical limitation **and declines to quantify it**. That is the correct handling and it exists in this literature. The search also found **no traceable derivation of the 10 ms value anywhere** — so **E-ASM-03's verdict is unchanged**, remains *"discrepancy requiring verification, not proven error"*, and must not be strengthened on the strength of this search — and **no paper reproducing LI-25's 16.76 ms at all**.

**CH-76 — ANJ-25 version-checked; ALE-26 closed as far as access permits.** The IEEE published GLOBECOM 2024 record is paywalled, but Semantic Scholar confirms `arXiv:2502.16472` **is** that paper, and the team's extended journal article was obtained in full from an open institutional repository. **All five load-bearing ANJ-25 claims survive in both versions** — 10.8 mm at 28 GHz, ≈3 dB, the 100-iteration cap with the −30 dB stopping rule, quasi-static flat fading, and perfect CSI (the journal adds an acquisition route the conference paper omits). **ALE-26:** `arXiv:2409.09590` is confirmed to be the 2025 AP-S/CNC-USNC-URSI paper; the published version is paywalled, and **ALE-26 supports no active manuscript claim** (its only row, E-59, is uncited in the manuscript), so nothing is exposed. **MA-26 remains a preprint with no version of record.**

**CH-77 — U12b decided: RETAIN.** The disclosure of this review's own retracted ≈1.25–3.75 s derivation stays in the manuscript body at §6.2 and §9.4. Applying the stated test, it materially strengthens the methodological argument rather than narrating the drafting process: §6.5 claims these substitutions follow from underspecified reporting rather than carelessness and closes with *"we are not accusing anyone of bad faith"* — a claim our own instance **demonstrates** where it would otherwise merely be asserted. If length pressure arises during authoring, compress §9.4's restatement, not the §6.2 blockquote. The audit trail in `citation_audit.md` C5 is permanent regardless.

**Manuscript changes in this pass.** §6.5 gained the forward-search result, the four-instance count, the escalations, the negative independence result and the counterexamples; §1 C3 was reworded to match; §9.4's "no forward citation search" limitation was discharged and replaced with the search's actual bound; §2 and §1 gained the RAN-25 version-of-record page locators. **No claim was strengthened beyond what the evidence supports, and C3 was deliberately not upgraded.**

**Files changed.** `manuscript/01`, `02`, `06`, `09`; `literature_search_log.md`, `evidence_matrix.md`, `source_inventory.md`, `unresolved_questions.md`, `novelty_boundary.md`, `manuscript_argument_map.md`, `absence_claims.md`, `draft_status.md`, `audits/citation_audit.md`, `audits/independence_audit.md`, `CHANGELOG.md`.

**Not done, deliberately.** Tables 1–7 and Figures 1–4; stylistic rewriting; shortening; CST material; OCR of the three image-only sources (no active claim depends on them); any attempt to manufacture uncertainty figures the sources do not report; any attempt to remove the domain-expert-review limitation.

---

## v1.5 — 18 August 2026 — freeze consistency gate

Scope: **verification that the files on disk actually carry the v1.4 freeze**, prompted by an external advisory reporting twelve pre-freeze statements still present. No new literature search, no stylistic rewriting, no figures, no bibliography formatting, no forward-citation search. Manuscript draft stays **v0.13**; documentation v1.4 → v1.5. Entries CH-65 … CH-69.

**CH-65 — The advisory's twelve reported mismatches were checked individually against the files and none was reproduced.** `01_introduction.md` carries `**Draft v0.13**`, contains no form of the word "incompatible", no "nothing at all below a millisecond", and no "overturns"; `05_hardware_evidence.md` carries the normalised reflection-amplitude wording and "On this platform, bare actuator response is not the dominant contributor to the closed-loop search time"; `timescale_matrix.md` carries neither "entire element-to-tile gap" nor the conditional-symbol-rate framing; `source_inventory.md` and `unresolved_questions.md` both record the RAN/YAN/XU part of U6 as closed; `evidence_matrix.md` carries version-of-record locators on E-01…E-05; `10_conclusion.md` says "require qualification". **There is exactly one documentation tree** — `<private working-repo path — redacted>` — and no duplicate or stale copy exists anywhere on the volume. The v1.4 edits had been written to disk correctly.

**CH-66 ‼ — A version-of-record failure was found that v1.4 missed: YAN-25's "125 %" gain does not exist in the published paper.** The preprint abstract states *"In a multi-element, multi-path scenario, the EM-only mode improves the received signal power by 125% compared to the PBF-only mode."* **The version of record deletes that sentence**, retaining only the qualitative comparison — PBF-only *"shows less effective than the EM-only mode in enhancing received signal strength"*, EM-PBF *"further enhances performance"* (p. 6823) — and a full-document scan finds **no percentage figure anywhere in its technical text**. The figure had been cited in §1.1 as one of three headline reported benefits. **It is withdrawn from the manuscript**, which now carries the version of record's qualitative statement; `evidence_matrix.md` **E-13** is reclassified **superseded-by-version-of-record** and retains the preprint wording as superseded; `source_inventory.md`, `unresolved_questions.md` U6, `audits/citation_audit.md` row 3 and `audits/evidence_audit.md` are corrected. **The figure must not be re-derived from a published plot** — that would be an upgrade of evidence. The same cross-version percentage scan was run on RAN-25 and XU-22: **both are consistent between versions**, so the failure is isolated to YAN-25.

**CH-67 — v1.4's claim that "every load-bearing quantity survived" is corrected.** It was written after checking the five YAN-25 items the review brief enumerated (protocol, Fig. 8, CPU, iteration cap, tolerance) and did not re-check that source's abstract-level performance figure. The accurate statement is: **every load-bearing timing and parameter quantity survives; one non-timing quantitative claim (E-13) did not.** Corrected in `unresolved_questions.md` U6 and `draft_status.md`.

**CH-68 — Five residual preprint locators were found and updated.** These sat in rows and files outside CH-51's propagation list, which covered E-01…E-05, E-12, E-14…E-18 and manuscript sections 01, 03, 04, 06 — skipping E-06, E-07, E-13, T-CH-05 and all of `02_architecture.md`. Old → new, each verified against the published PDF: **E-06** waveform-rate equivalence, p. 8 → **p. 13328**; **E-07** MUSIC spectra, pp. 9–11 Figs. 4–7 → **pp. 13330–13331, Figs. 6–9** (figure numbers shifted by two); **T-CH-05** protocol rates, pp. 7–8 → **p. 6829**; `02_architecture.md` mechanical-platform citation, p. 2 → **p. 13320**; and `Eqs. (1a)–(3)` gained its page, **p. 13321**, in both `01_introduction.md` and `02_architecture.md`. `source_inventory.md`'s YAN-25 simulation-setup locator, p. 11 → **pp. 6832–6833**.

**CH-69 — Version markers reconciled.** Documentation v1.4 → **v1.5**; manuscript stays **Draft v0.13** (its scientific content is unchanged apart from the E-13 withdrawal, which removes a claim rather than adding one). `draft_status.md` updated.

---

## v1.4 — 17 August 2026 — version-of-record integration and scientific freeze

Scope: **integration of three published full texts obtained by the user, plus the corrections that follow from them.** No new literature search, no stylistic rewriting, no restructuring, no shortening, no CST material, no figures, no bibliography formatting. Manuscript draft v0.12 → v0.13. Fifteen entries, CH-50 … CH-64.

> ✅ **Three versions of record were obtained, archived and inspected on 17 August 2026**, and read directly from the PDFs rather than from metadata:
> - `01V_Ranasinghe_High_Mobility_FIM_ISAC_IEEE_TWC_2026_VERSION_OF_RECORD.pdf` — *IEEE TWC* **25**, 13319–13335, 2026 · 10.1109/TWC.2026.3668992
> - `04V_Yang_FIM_Architecture_and_Performance_IEEE_TWC_2026_VERSION_OF_RECORD.pdf` — *IEEE TWC* **25**, 6823–6836, 2026 · 10.1109/TWC.2025.3627095
> - `07V_Xu_High_Mobility_RIS_Channel_Estimation_IEEE_TVT_2023_VERSION_OF_RECORD.pdf` — *IEEE TVT* **72**(1), 718–734, **Jan 2023** · 10.1109/TVT.2022.3203818
>
> The earlier preprint/draft copies are **retained, not deleted**, and are recorded as earlier versions within the same independence group (IG-1, IG-3, IG-4). They are never counted as independent evidence.

**CH-50 ★ — Every load-bearing quantity survived version-of-record checking.** Verified directly against the published PDFs: RAN-25's Table III (28 GHz, λ = 0.0107 m, B = 20 MHz, N_T = N_R = 4, V_max = 208 m/s, morphing range −λ to +λ) and footnote 10 (shape reuse, verbatim); YAN-25's protocol statement, Fig. 8 running-time experiment, i7-13650HX CPU, 400-iteration maximum and 10⁻⁸ tolerance; XU-22's 2.6 GHz / 90 mph / 51-symbol window, its Fig. 4(b) 20-symbol @ 0.95 and 40-symbol @ 0.82 cases, and its (1 + M) ON/OFF slots. **No quantitative claim required correction.**

**CH-51 — Locators updated to the version of record.** Old → new: RAN-25 **Table II, p. 8 → Table III, p. 13327**; **footnote 8, p. 8 → footnote 10, p. 13327**; Eqs. (1a)–(3), p. 3 → **Eqs. (1c), (2), (3), p. 13321**; Fig. 2 discussion, p. 8 → **pp. 13327–13328**. YAN-25 **pp. 7–8 → p. 6829**; **Fig. 8, p. 11 → Fig. 8, p. 6833**. XU-22 **p. 4 → p. 721**; **Fig. 4b, p. 9 → Fig. 4(b), p. 728**; 1 + M slots **pp. 3, 6–8 → pp. 720, 722, 726**; simulation table **p. 10 → p. 727**. Propagated through `manuscript/01`, `03`, `04`, `06`, `evidence_matrix.md` (E-01…E-05, E-12, E-14…E-18, T-ASM-01, T-YA-01), `source_inventory.md`, `timescale_matrix.md`, `unresolved_questions.md` U6 and `audits/citation_audit.md`.

**CH-52 ‼ — A material addition was found in RAN-25's version of record, and it cuts both ways.** A **Remark on pp. 13321–13322, absent from the held arXiv preprint**, states that distributed Lorentz forces *"enable rapid reconfiguration of the FIM surface shape within millisecond switching speeds [36], comparable to typical channel coherence times in high-mobility wireless scenarios"* — reference [36] being BAI-22 — and then that *"the FIM surface shape can also be morphed once over several channel coherence intervals to achieve statistically optimal performance, thus offering a flexible trade-off between achievable performance gains and morphing speed requirements."* Registered as **E-03b** and **E-03c**, and added to §6.5 and §9.1. Consequences: (i) the two-timescale premise is **stated by the system literature itself**, not reconstructed by us; (ii) the millisecond assertion is a **further instance of the substitution §6.5 documents**, since BAI-22's complete package reports element response < 0.07 s and system morphing within 0.1 s, its fastest quantity measured with a 60 fps camera. **Recorded as a discrepancy requiring verification, not as an error** — the same standing rule as the tabulated 10 ms value.

**CH-53 ‼ — Two false "nothing below a millisecond" statements corrected.** The abstract-adjacent §1.1 said *"nothing at all below a millisecond on any RF aperture"* and §3.2 said the reviewed set contains *"no measurement of any mechanical or control process on an RF aperture completing in under a millisecond"*. **Both are false**: AKR-26 reports a per-element electronic FPGA update below 0.1 ms on an RF RIS. The AKR-26 comparator is preserved and now stated explicitly. The absence actually supported is narrower and is what the manuscript now claims: **no reviewed measurement demonstrates sub-millisecond commanded mechanical morphing of a flexible RF aperture, nor a sub-millisecond aperture-level interval from a commanded geometry change to stabilised RF operation.**

**CH-54 ‼ — 16.76 ms is no longer called a "complete loop".** §3.2 called it *"the fastest complete measured loop on a flexible surface"*, violating the manuscript's own timing taxonomy. 16.76 ms runs from **shape acquisition to bias-voltage supply** and is a measured **multi-stage partial electronic compensation chain**. The separate 16.7 ms experiment reaches **sensor trigger → stabilised RF output**, under **static geometry**. Both are now named at their own scope in §1 (abstract and C2), §3.2 and §6.3; `novelty_boundary.md` and `manuscript_argument_map.md` were corrected in the same terms. **The two values are never substituted.**

**CH-55 — Residual universal fastest/slowest labels removed from §3.3.** The section still called fast fading *"the fastest quantity in the system"* and long-term spatial correlation *"the slowest quantity"*, and then stated that no universal ordering is imposed. Both categorical labels are deleted. The conditional forms now stand: instantaneous/small-scale fading is the fast-varying state that motivates the problem; statistical-CSI formulations **deliberately treat** channel statistics as slower-varying than instantaneous fading; angular evolution and target kinematics have scenario-dependent validity intervals. `manuscript_argument_map.md` §3 claim 4, which still read *"Only the first is fast"*, was corrected with a standing prohibition.

**CH-56 — Remaining Akram contradictions repaired in supporting documentation.** The manuscript was already correct after v1.3; the documentation was not. Removed: *"on the tile-based platform it accounts for the entire element-to-tile gap"* (`timescale_matrix.md`); *"differ by 100× … because control networks serialise"* (`novelty_boundary.md`); *"the 100× element/tile gap and its cause"* and *"because control networks serialise"* (`manuscript_argument_map.md`). The safe statement everywhere is now: AKR-26 reports **two differently-scoped upper bounds** and **separately** a ×4 multiplexing penalty; together these establish that panel-scale timing cannot be inferred from element timing and that the control architecture introduces measurable overhead; **they do not establish a decomposition of the full tile-level interval.**

**CH-57 — Exact "100×" replaced throughout.** Because both AKR-26 values are upper bounds, no exact measured ratio may be stated. §8.2's Group A motivation now reads *"element- and tile-level update bounds at different scopes, differing by up to two orders of magnitude"*; `audits/redundancy_audit.md` was re-worded in the same terms.

**CH-58 — §6.3's non-commensurability rule made precise instead of self-contradictory.** The paragraph forbade ordering, differencing and interpolation and then immediately observed the numerical ordering. The rule now reads: **these heterogeneous measurements may be displayed on a common numerical axis to show that their reported ranges overlap, but must not be treated as like-for-like performance measurements, pooled statistically, or used to infer the bottleneck of an integrated architecture.** The observations that 72 ms > 16.76 ms and that 72 ms is within about a factor of four of ≈300 ms are compatible with it. Propagated to `timescale_matrix.md` Category 4 and `audits/latency_definition_audit.md` §3.

**CH-59 — "The simplification fails" weakened to the strongest defensible result.** Headings changed: §6.3 *"The first simplification that fails: 'fast electronics, slow mechanics'"* → **"Why 'fast electronics, slow mechanics' requires qualification"**; §6.4 *"The second simplification that fails: latency as a property of technology"* → **"Why a latency is a property of an object, not of a technology"**. The abstract, C2, §10 and `novelty_boundary.md` §5 now say the simplifications **require qualification** rather than that they fail or are overturned. The defensible result stated everywhere: **architecture labels alone do not establish a universal latency ordering; "fast electronics, slow mechanics" requires mechanism-, object- and architecture-specific qualification.** The numerical-overlap observation is preserved; no bottleneck is inferred for an integrated FIM.

**CH-60 — LI-25's reflection-amplitude floor is no longer called an efficiency figure.** §5.1 described the >0.4 normalised reflection amplitude as *"the only efficiency figure available for a flexible reflective aperture in the reviewed set"* and inferred substantial loss. The source does not define the quantity as an efficiency. It is now reported as a **normalised reflection-amplitude floor** *"indicating non-unity, bias- and incidence-dependent reflected amplitude"*, with an explicit statement that we do not convert it into a radiation or reflection efficiency.

**CH-61 — The Bai actuator sentence toned to what the evidence establishes.** §5.3's *"Nothing is slow about the actuator"* → **"On this platform, bare actuator response is not the dominant contributor to the closed-loop search time."**

**CH-62 — "Four mutually incompatible architectures" replaced.** Now **"four physically non-equivalent architectures whose timing quantities are not directly interchangeable"**, in the abstract, C1, §10, `novelty_boundary.md` N1 and `manuscript_argument_map.md`. "Incompatible" could be misread as claiming these technologies cannot be physically integrated, which this review does not establish.

**CH-63 ‼ — Two statements the review made about its own sources were wrong, and both are corrected.**
1. **The ≈0.51 ms conversion was never conditional.** U5, `audits/latency_definition_audit.md` F2, `audits/citation_audit.md` row 8, `audits/evidence_audit.md`, `draft_status.md` §3 and `evidence_matrix.md` E-DR-02 all recorded that converting 51 symbols to wall-clock time *"requires an assumption the source does not state (symbol rate = bandwidth)"*. **XU-22 states f_s = 100 kHz explicitly** in its system model — in the version of record (p. 721) **and in the held draft (p. 4)** — and uses f_d = v f_c/(c f_s) throughout. The error was ours and predates the version check. All six records corrected.
2. **The conversion is nonetheless deleted from the manuscript**, resolving U5. The decision is presentational, not evidential: the 51-symbol figure carries the argument, and a derived wall-clock number invites exactly the decontextualised quotation the manuscript criticises. It is retained in `evidence_matrix.md` E-DR-02 and `timescale_matrix.md` Category 1, annotated *"derived from the source-stated f_s = 100 kHz; not required by the manuscript argument."*

**CH-64 — Blocker list updated, and the forward-citation search's purpose corrected.** U6's RAN/YAN/XU half is **closed**; what remains under U6 is the published full texts of ANJ-25 and ALE-26 (MA-26 has no journal version of record). §9.4, §10, `draft_status.md` and `source_inventory.md` no longer state that these full texts are unobtained. Separately, four records claimed the forward citation search was *"the only route"* to the tabulated 10 ms value's derivation. Corrected in `absence_claims.md` §4, `literature_search_log.md`, `evidence_matrix.md` E-ASM-03 and `unresolved_questions.md` U3: the search **may quantify propagation of the Ni and Bai timing values and identify further independent uses of them; it does not necessarily resolve the origin or derivation of Bai's 10 ms value.** **The Bai verdict remains "discrepancy requiring verification, not proven error"** and should be expected to remain so regardless of the search's outcome. The search should now also cover RAN-25's own millisecond assertion (E-03b).

**Bibliography years.** `RAN-25` → **IEEE TWC 2026**; `YAN-25` → **IEEE TWC 2026**; `XU-22` → **IEEE TVT 2023**. Internal keys are unchanged to avoid destructive re-keying, but **no sentence in the manuscript describes Xu as a 2022 publication.** `ANJ-25` → GLOBECOM **2024**; `ALE-26` → **2025**.

**Files changed in v1.4:** `manuscript/01`, `03`, `04`, `05`, `06`, `08`, `09`, `10`; `source_inventory.md`, `evidence_matrix.md`, `timescale_matrix.md`, `novelty_boundary.md`, `manuscript_argument_map.md`, `unresolved_questions.md`, `absence_claims.md`, `literature_search_log.md`, `draft_status.md`, `CHANGELOG.md`, `audits/citation_audit.md`, `audits/latency_definition_audit.md`, `audits/redundancy_audit.md`, `audits/evidence_audit.md`. Three PDFs **added** to the corpus (`01V_`, `04V_`, `07V_`); **no existing corpus file was modified, renamed or deleted.**

**Not done, deliberately:** no new literature search, no figures generated, no bibliography formatted, no forward citation search, no restructuring, no shortening, no submission-readiness claim.

---

## v1.3 — 17 August 2026 — manuscript-level correction pass following full-text independent review

Scope: **manuscript-level overclaims only.** No new literature search, no expansion, no stylistic rewriting, no CST material. Manuscript draft v0.11 → v0.12. The reviewer read Sections 01–10 in full rather than the audit documentation, and found conclusions stronger than the evidence system permits. Twelve corrections.

**CH-38 ‼ — The 28-GHz Doppler figure no longer implies an interval, a criterion or a verdict.** v0.11 had already stopped writing "tens of microseconds", but four weaker conversions survived. `manuscript/03` §3.2 said *"Under any conventional correlation criterion, a fast-fading process with a 19 kHz Doppler spread decorrelates on a timescale far shorter than the intervals the reviewed hardware reports"* and concluded *"FIMs at this operating point cannot be adapting per fading realisation"*; the abstract said delay–Doppler channels *"vary on timescales far shorter than any mechanical process"*; §10 said *"varying on microsecond timescales"*. Each derives a coherence claim from f_D alone. All four are replaced. The 19.4 kHz figure is retained and now establishes a **strongly time-selective regime**, from which no coherence interval and no update deadline follow without modelling choices the reviewed sources do not make. The inference is stated as evidential: **the reviewed hardware does not establish support for per-fading-realisation mechanical adaptation at this operating point**, and this review does not claim such adaptation is physically impossible. XU-22's 51-symbol result is retained unchanged as a separate high-mobility example at its own carrier, velocity and threshold.

**CH-39 — Angular and statistical timescale ordering made conditional.** §3.3 asserted *"Angular drift is slower than fading by orders of magnitude in typical geometry"* and *"Only the first of these four is fast."* Neither is supported by a cited primary source or a defined geometry. Replaced with the supported point: the four processes **need not share one validity timescale**, and statistical-CSI approaches deliberately exploit slower-varying channel statistics to reduce geometry-update frequency. No universal ordering is imposed. §3.6 and the §3.3/§3.4 headings were reconciled ("deadlines" → "timescales").

**CH-40 ‼ — §9.1's "and only those" is withdrawn.** The fast, channel-dependent path is now stated as **channel estimation, fast-state optimisation/inference, command transmission and electronic update**, explicitly conditional on the two-timescale premise, with the geometry stages exempt *under that premise only*. The premise is labelled as assumed by the system literature and not established, so §9 no longer presupposes it as a finding.

**CH-41 — §9 reconciled with §6, in three places.** (a) §9.1 said channel acquisition is supported only by simulation and then that *"the fast requirements are supported by measurements on rigid panels"* — a contradiction. Demand (analytical/simulated) and electronic-update hardware evidence (measured on rigid panels) are now separated, with the note that the comparison inherits the weaker evidence type. (b) §9.2's *"the two-timescale framing works cleanly … it does not work for liquid crystal"* is deleted; electronic update latency is now **mechanism-dependent**, with neither mechanism labelled adequate or inadequate, since no scenario-specific deadline exists. (c) §9.2's *"command distribution … dominates"* and *"the cost that dominates real hardware"* are withdrawn and matched to §6.4: a **major contributor, absent from the FIM system models reviewed here.**

**CH-42 ‼ — The Akram causal claim is withdrawn.** The two source facts are preserved (< 0.1 ms per element; < 10 ms per 16 × 16 tile), as is the reported ×¼ time-multiplexing penalty. What is removed is the inference that time-multiplexing *explains* the full up-to-100× interval: the two figures are **upper bounds on differently-scoped objects**, the stated penalty is a factor of four rather than one hundred, and the paper does not decompose the tile-level bound. The comparison now establishes only that an element-level update time cannot substitute for a tile-/aperture-level configuration time and that array-scale command distribution carries architecture-dependent overhead. Propagated through the abstract, §1.2, C2, §5.2, §6.4, §8.2 Group D, §9.2, §10 and `timescale_matrix.md` Category 3.

**CH-43 ‼ — λ/20 is no longer a prescribed settling threshold.** §8.1 states that the framework prescribes definitions and not acceptance thresholds, yet §§7.2, 7.4, 8.2 and 9.3 recommended settling against λ/20 — internally inconsistent, and no primary source or derived phase-error requirement justifies that exact value. The requirement is now **a stated and justified electromagnetic settling criterion**, expressed as an allowable phase/RF-performance error or the corresponding displacement tolerance relative to wavelength. λ/20 survives in exactly one place, §7.2, explicitly labelled an illustration of the *form* of such a criterion and not a recommended value. The proposed experiment is no longer instructed to use it.

**CH-44 — §8 reporting-framework consistency, six repairs.** (a) *"Every timing value in this review was measured competently"* deleted — the register contains measured, simulated and projected values and several without uncertainty; replaced with the neutral definitional statement. (b) Fields are now motivated by *"a recurring reporting deficiency or a specific unresolved quantity"*, not by quantities "no reviewed study reports"; propagated to C4 and §10. (c) The Group-B end-event motivation was stale after the Bai supplement: it now records **two** stated but incommensurable criteria — the LC 10 %/90 % thresholds and BAI-22's first-frame-with-no-visible-deviation on a 60 fps camera, with its ≈16.7 ms floor — while retaining the larger point that thresholds are inconsistent or absent for many intervals. (d) Group D: *"dominate real panels"* → *"can materially contribute to panel-scale latency"*. (e) Group E: *"unmeasured everywhere"* is false after LI-25 Supplementary Note 6 and now reads that the interval to stabilised RF **has been measured for static-geometry electronic compensation** and **remains unreported following a commanded geometry change**. (f) *"the great majority of its fields are quantities the authors already measured but did not report"* was never demonstrated; replaced with *some could be reported from existing measurements, others require new ones.*

**CH-45 ‼ — The quadrant map is repaired and "fixed curvature is solved" is withdrawn.** §2.5's heading becomes *"Fixed-curvature conformal design has substantial prior art"*, and claims that curvature, incidence and quantisation are "solved" or "handled" are replaced with statements that validated methods and measured hardware exist for important fixed-curvature cases — in `manuscript/02`, `architecture_taxonomy.md`, `evidence_matrix.md` E-54, `manuscript_argument_map.md` and `audits/independence_audit.md`. Separately, the two-axis map was **self-contradictory**: it labelled the lower half "geometry is EXOGENOUS" while A1 makes geometry the primary optimisation variable. The map is now restricted to **demonstrated hardware platforms**, with axes *commanded physical geometry demonstrated?* and *RF performance measured?*, and **A1 is represented as the modelled target operating point that names the empty cell**, never as a platform occupying one. Prose, the Figure 1 specification and the taxonomy documentation now agree.

**CH-46 — The abstract's "all of which already exist" is removed.** It conflicted with the manuscript's central statement that no reviewed hardware platform combines commanded geometry with measured RF operation. Replaced with *"We propose no new hardware and no new control algorithm, and we make no novelty claim for the two-timescale or statistical-CSI principle."* A standing wording constraint is recorded in `novelty_boundary.md`.

**CH-47 — Field-level criticism narrowed to the cases traced.** The traceability audit rests on two system papers that share authors, with no forward citation search completed. Field-level phrasing was replaced with *"the system papers audited here"*, *"the specific feasibility claims examined here"* and *"in the cases traced here"* in the abstract, C3, §4.5, §5.5, §6.4, §6.5, §7.5, §9.5 and §10. **The confirmed Ni substitution is not weakened; the Bai discrepancy is not strengthened to an error; the forward citation search remains a pre-submission blocker.**

**CH-48 — Speculative "cheap experiment" language removed.** *"All that is missing is a stopwatch"*, *"requires no new apparatus"*, *"nearly free"* and *"closable today"* are replaced in §§5.4, 7.4, 9.3 and §10 with the conditional formulation: **if the existing experimental platform remains available**, adding synchronised timing of curvature change to re-converged RF performance could close this gap without a new aperture prototype. Access, trigger instrumentation, synchronisation and reproducibility are stated as unknown. The recommendation itself is retained.

**CH-49 ★ — Publication metadata resolved for all six U6 preprints, from publisher-deposited Crossref records.** RAN-25 → *IEEE TWC* **25**, 13319–13335, 2026, DOI 10.1109/TWC.2026.3668992. YAN-25 → *IEEE TWC* **25**, 6823–6836, 2026, DOI 10.1109/TWC.2025.3627095. XU-22 → *IEEE TVT* **72**(1), 718–734, **Jan 2023**, DOI 10.1109/TVT.2022.3203818. ALE-26 → *2025 IEEE AP-S/CNC-USNC-URSI*, 1–4, DOI 10.1109/AP-S/CNC-USNC-URSI55537.2025.11265965. MA-26 → **no journal version found**; only TechRxiv DOI 10.36227/techrxiv.176857884.40392967/v1. **ANJ-25 was mis-recorded** and is corrected: the held copy is not a 2025 preprint but the published **GLOBECOM 2024** paper, pp. 4932–4937, DOI 10.1109/GLOBECOM52923.2024.10901792, and the TWC 24(4):2940–2955 article is a **separate** publication by the same team, not its version of record. Three internal keys now carry years that differ from their versions of record (`XU-22`→2023, `ANJ-25`→2024, `ALE-26`→2025); keys are retained to avoid re-lettering the documentation set, and the bibliography must use the version-of-record year.
> ⚠ **Metadata only, and this is stated wherever it is recorded.** Crossref records and IEEE Xplore listings were seen; **no published full text was retrieved** — Xplore returned no content to our fetches. **No quantitative claim has been re-verified against any version of record.** RAN-25's journal version is 17 pages against the held copy's 12, so Table II and footnote 8 — the review's most load-bearing quotation — remain unchecked. U6 stays a current blocker for that reason.

**Also fixed during the sweep, not on the reviewer's list.** §3.4's heading claimed overhead *"consumes the budget"*, presupposing the budget the review declines to define; and `timescale_matrix.md` Category 3 asserted the AKR-26 pair was *"the same nominal operation"* differing only in object — false, since configuring 256 elements is not the operation of switching one — and named T-AK-03 as the explanation. Both corrected.

**Files changed in v1.3:**
all ten manuscript sections, `manuscript/01`–`10`; `source_inventory.md`, `unresolved_questions.md`, `timescale_matrix.md`, `evidence_matrix.md`, `architecture_taxonomy.md`, `novelty_boundary.md`, `manuscript_argument_map.md`, `draft_status.md`, `CHANGELOG.md`; `audits/independence_audit.md`.

**Not done, deliberately:** no figures generated, no references formatted, no new literature search, no submission-readiness claim.

---

## v1.2 — 17 August 2026 — cross-file consistency repair following second independent review

Scope: **stale contradictions only.** No new sources, no literature search, no stylistic rewriting, no expansion, no change of research direction. Manuscript draft v0.10 → v0.11.

The v1.1 pass corrected the manuscript's §6 conclusions but left several documentation files still asserting the superseded versions. A reviewer inspecting the documentation would have found it contradicting itself. Eight repairs.

**CH-29 ‼ — The three-level replication claim survived in three files and is now removed everywhere.** `evidence_matrix.md` Part C still licensed "element-level … consistently faster than full-surface … consistently faster than closed-loop convergence … supported independently by BAI-22 and NI-22 and is the quantitative backbone of Section 6", and `independence_audit.md` still said "This is why §6.2 can call it 'replicated'." Both directly contradicted `evidence_audit.md` and `latency_definition_audit.md`, which had already withdrawn the claim. Part C now licenses only: element and surface are distinct quantities; within each platform the element value is below the surface value; the step magnitude differs strongly between platforms; closed-loop convergence is BAI-22 alone; **the full three-level ordering is not independently replicated.** `timescale_matrix.md` Categories 5–7 were reconciled to the same statement.

**CH-30 — Stale supplement state cleared.** `source_inventory.md` still carried "**The supplementary file is NOT in the corpus**" for LI-25 and "not in the corpus" for BAI-22; `evidence_matrix.md` E-ASM-03 still said "pending supplementary verification"; `literature_search_log.md` still recorded the caveat that the Bai supplement might contain a 10 ms figure; `absence_claims.md` §4 still listed retrieval as a pre-submission requirement blocking A7; `draft_status.md` still referred to "unavailable supplements". All updated to retrieved. **The Bai verdict is unchanged and was deliberately not strengthened**: still a discrepancy requiring verification, not an error — the reason having shifted from "the cited paper is unavailable" to "the citing authors' derivation is unobservable". A7 is now blocked on the forward citation search alone.

**CH-31 ‼ — "Contiguous" was mathematically false and is removed.** `adaptation_chain_matrix.md` §3.1 described LI-25's S1→S2→S4→S5→S6 as "the longest measured contiguous run". That sequence **skips S3** and is not contiguous under this project's own taxonomy. Replaced with a statement of extent: **LI-25 measures the largest set of linked stages on one flexible RF platform, covering S1–S2 and S4–S6, while S3 channel estimation is absent.** Propagated to `manuscript/01` (abstract and C1), `manuscript/04` §4.3 (including its heading), `manuscript_argument_map.md` §4 and `novelty_boundary.md` N1. **N1 no longer depends on contiguity** and is not weakened by the change: what makes the decomposition informative is which stages are missing and that the measured ones sit on incompatible platforms. A wording constraint was added to N1 forbidding the term's return.

**CH-32 — The S10 distinction was propagated.** `adaptation_chain_matrix.md` §3.4 still listed S10 onset among quantities "unreported by *every* source", which T-LI-06 falsified. It now distinguishes (1) **RF stabilisation under electronic compensation with static geometry — measured**, 16.7 ms, from (2) **RF stabilisation following a commanded mechanical morph — not found in the reviewed set**. Only (2) is a gap.

**CH-33 — LI-25's S10 matrix cell now records both evidence modes.** The cell read `D` on the strength of the video-link result alone. It now reads `D + M‡` with a condition note: **demonstrated** under dynamic bending (untimed) and **measured** under static geometry (16.7 ms, T-LI-06). The note states explicitly that the matrix records **no** S10 timing following deformation — the only experiment containing a shape change is untimed, and the only one carrying a number holds the shape fixed. A multi-code convention was added to the legend.

**CH-34 — Over-transfer from BAI-22 removed.** `adaptation_chain_matrix.md` §3.5 asserted "This is a general and transferable observation … Any FIM architecture that needs closed-loop geometry verification inherits this problem." One platform cannot establish that. Rewritten as a **design warning**: in BAI-22's demonstrated loop, sensing, perception and search exceed the bare actuator response; whether that holds in an RF FIM **must be measured rather than assumed**. Two equivalent instances in the manuscript body (§4.6, §5.3) were found and rewritten the same way. What survives as general is only the negative point: no A1 paper models the stage, so none would detect the problem.

**CH-35 — `timescale_matrix.md` reconciled with the revised §6.3.** Category 4 still read "'Electronic control is fast' is therefore true only for semiconductor tuning; it is false for liquid crystal" — a universal ordering by architecture label, which §6.3 now refuses to draw. Replaced by the overlap observation plus the explicit non-commensurability of the three comparability classes involved, and a prohibition on inferring the bottleneck of an integrated FIM that has never been built. **The "four orders of magnitude" span is withdrawn** in Category 4, `manuscript/05` and `evidence_strength_matrix.md` C6: it has **no quantified lower endpoint**, LI-25 calling varactor switching "negligibly short" with no number. The quantified values (≈15 ms to tens of seconds) are all liquid crystal.

**CH-36 — Doppler is no longer converted directly into a time budget.** `timescale_matrix.md` Category 1 statement 2 read "…i.e. a fast-variable timescale far below a millisecond", and `manuscript/01` §1.1 and `manuscript/03` §§3.2–3.3 turned 19.4 kHz into "a budget measured in tens of microseconds" and "a per-fading-realisation deadline". A three-row table now separates **Doppler frequency** (derived, exact), **correlation/coherence interval** (threshold-dependent; not held at 28 GHz — the only quantified figure is XU-22 at 2.6 GHz), and **update deadline** (defined nowhere in this review, by design). The ≈19.4 kHz figure is retained. The comparison it supports is between a decorrelation timescale and measured intervals, **not** a shortfall against a deadline.

**CH-37 — Historical entries marked, not erased.** CH-15's stated reason ("the relevant supplementary material is unavailable") is superseded by CH-20 but retained with a superseded-marker, per the audit-transparency rule: completed blockers remain in the changelog as historical facts and must not remain listed as current blockers anywhere else. The retracted ≈1.25–3.75 s derivation likewise remains recorded in eleven places, all of them retraction notices, guards or disclosures — none of them active use.

**Also fixed during the sweep, not on the reviewer's list:** `manuscript/07` §7.5 still appealed to "the ordering established in Section 6.2 — tens of milliseconds at element level, hundreds at surface level, **seconds under closed-loop verification**", carrying both the withdrawn three-level ordering and the retracted seconds-scale value into the manuscript's closing argument for feasibility. Rewritten to cite the per-platform values it can support and to exclude the closed-loop level explicitly.

---

## v1.1 — 17 August 2026 — surgical correction pass following independent adversarial review

Scope: targeted technical corrections plus two source retrievals. **No broad rewrite, no re-review, no new novelty claims, no CST reinstatement.** Manuscript draft v0.9 → v0.10.

### Sources retrieved and archived (4 new files in `Metasurfaces/`)

**CH-18 ★ — The Fan Li supplement was obtained. CH-10 is resolved.** Retrieved from the publisher's open-access supplementary endpoint and archived as `23_FanLi_FISP_Supplementary_Information.pdf` (33 pp.) with `23B_FanLi_FISP_Peer_Review_File.pdf` (49 pp.). Supplementary Note 6 establishes: T1 = 4 ms and T2 ≈ 2 ms are **thread-timing averages**, T3 = 5.25 ms is an **oscilloscope measurement**; 16.76 ms exceeds the 11.25 ms sum because of **full-cycle control of 32 channels over RS-232 at 115 200 bps**; a **60 Hz** deformation-rate limit follows; and a projected < 10 ms is explicitly not built. **No repetitions, sample size or uncertainty are reported for any headline interval.** Note 7 confirms the mechanical platform is a manually-adjusted telescopic-link/fulcrum fixture with no actuation timing, vindicating the A3 classification.

**CH-19 ‼ — A previously unknown end-to-end measurement was found.** Supplementary Note 6 reports a second, independent experiment: a finger-triggered sensor, a dual-channel oscilloscope, and an AD8317 logarithmic detector at 3.1 GHz, measuring **16.7 ms from trigger to stabilised RF output**. This is the only interval in the corpus whose end event is stabilised radiation. **It falsified T-GAP-03 as previously worded.** T-GAP-03 was narrowed to "RF stabilisation following a *commanded geometry change*", gap G4 was rewritten, capability C14 was upgraded to L5/L6 with a new C14b for the residual gap, and absence claim A16 now forbids the old wording. The geometry is static throughout the measurement, so the FIM-relevant interval remains unmeasured.

**CH-20 ★ — The Bai supplementary package was obtained. CH-15's evidential basis is much stronger; its verdict deliberately did not change.** Archived as `19S_…Supplementary_Information.pdf` (71 pp.) and `19SB_…Peer_Review_File.pdf` (28 pp.); the Extended Data figures were already in the held main-text PDF. A full-package search returns **no occurrence** of "10 ms", "sub-element", "filament", "transient" or "rise time"; the unit "ms" does not appear in the SI at all; the sole "0.01 s" is a ± dispersion. Additionally, the element response was measured with a **60 fps** camera whose ≈16.7 ms frame period is coarser than the tabulated 10 ms. **The finding remains "a discrepancy requiring verification", not an error**, because the tabulating authors' derivation is not observable — per the reviewer's explicit instruction and the audit's own standard.

### Corrections to this review's own record

**CH-21 ‼★ — RETRACTION. A derived value of ours was wrong by a factor of about forty.** The draft derived closed-loop convergence as ≈1.25–3.75 s by multiplying BAI-22's "around 0.25 s" feedback cycle by its "5 to 15 iterations". **An iteration is not a cycle**: it comprises 4(N + M) + 2 = 34 function evaluations for a 4 × 4 sample, so 5–15 iterations is 170–510 cycles, and the supplement states convergence directly as **≈2.5 min**. E-DR-03 is retracted; T-BA-04 (iteration cost), T-BA-05 (convergence), T-BA-06 (10 fps open-loop replay) and T-BA-07 (60 fps instrument resolution) were added; a guard was added to `evidence_matrix.md` Part C. **This is the same species of substitution error the manuscript documents in others, committed by this review on a source it held in full.** It is disclosed in the manuscript body (§6.2, §9.4), not only in the audit trail. See `audits/citation_audit.md` C5.

**CH-22 — Absence claim A14 was falsified and rewritten.** The claim that neither mechanical platform reports a cycle count was **false**: BAI-22 SI S5.4 reports 1 000 reversible actuation cycles at constant amplitude (and 500 at higher current), and LI-25 SI Note 4 reports 3 000 bending cycles of the sensor array. C18 was upgraded to L4 and a new C18b isolates the actual gap — **no source pairs a cycle count with an RF degradation measurement.**

### Manuscript corrections (all requested by the independent review)

**CH-23 — §6.2 no longer claims a replicated three-level ordering.** The "one robust ordering" framing, the "roughly an order of magnitude" step claim and "the strongest replicated result" are withdrawn. Grounds: BAI-22's < 0.07 s → within 0.1 s is not an order of magnitude; closed-loop convergence is demonstrated by BAI-22 alone; and the convergence value itself was wrong (CH-21). The section now asserts only that **response time depends materially on the object timed and the completion criterion**, and that the three quantities cannot be substituted. NI-22's ≈30 ms → ≈300 ms step is retained as a **source-specific example**.

**CH-24 — §6.3's design conclusions were deleted.** "A liquid-crystal FIM would be limited by its material, not by its actuator" and "a varactor or PIN-diode FIM would be limited by its actuator and its control network" are removed: the reviewed set contains no integrated LC FIM and no mechanically-morphing radiating aperture, so no bottleneck can be established from measurements of separated parts. The overlap observation and every non-commensurability caveat are retained and strengthened — the three comparability classes are now named explicitly and the conclusion is stated as a **prohibition** on label-based latency ordering.

**CH-25 — §6.4 no longer claims universal dominance.** "The dominant cost is distributing commands across the aperture" became "aperture-scale command distribution **can be a major — and in some implementations potentially dominant —** contributor", because LI-25's transport share is ≈5.5 ms of 16.76 ms: a large individual contribution, not a majority. The under-modelling point, which does not depend on magnitude, is preserved.

**CH-26 — §6.6 was rebuilt.** (a) Simulated channel-acquisition evidence is no longer described as "measured support"; the prose now states that the fast-timescale *demand* rests on modelling, which is weaker evidence than the supply side. (b) "Adequate" / "inadequate" verdicts on electronic and LC updating are removed — sufficiency requires a deadline for the same state variable, architecture, carrier, mobility and correlation criterion, which no source supplies, and inventing one would contradict §6.1 and §8.1. (c) The mechanical row's categorical "No" became **"not necessarily — no, *if* geometry is optimised from statistical or geometric state"**, with the assumption named in the cell; a table convention note now states that the fast-fading column records consequences of assumptions, not findings. (d) S7–S10 now distinguish **capability demonstrated / timing measured / timing absent / architecture mismatch**, and the section closes by stating that an unmeasured quantity has no value, not a bad one.

**CH-27 — N3 rewording.** "It tells the reader which number to use" is removed — it contradicts the manuscript's own finding that there is frequently no single interchangeable number. N3 now states that the audit **determines what physical process each reported number measures, which substitutions are unsupported, and which quantities remain unmeasured**, and it explicitly preserves the asymmetry between the *confirmed* Ni substitution and the *unresolved* Bai discrepancy.

**CH-28 — Title changed.** Adopted: **"Flexible Intelligent Metasurfaces for High-Mobility ISAC: Hardware Evidence, Adaptation Timescales, and Validation Gaps."** Checked against the manuscript architecture — the three clauses map onto §5, §6 and §7 — and found accurate. The interrogative "What Has Actually Been Measured?" is retained in `article_type_assessment.md` §4 as a recorded alternative, not in use. The originally rejected working title is not reinstated.

### Files updated in this pass
`evidence_matrix.md`, `novelty_boundary.md`, `absence_claims.md`, `timescale_matrix.md`, `evidence_strength_matrix.md`, `architecture_taxonomy.md`, `source_inventory.md`, `literature_search_log.md`, `article_type_assessment.md`, `manuscript_argument_map.md`, `unresolved_questions.md`, `draft_status.md`, `CHANGELOG.md`; `audits/citation_audit.md`, `audits/latency_definition_audit.md`; `manuscript/01`, `04`, `05`, `06`, `07`, `08`, `09`, `10`.

**Still true: no original research file was modified, renamed or deleted.** The four retrieved PDFs are *additions* to `Metasurfaces/`; nothing pre-existing was touched, and the workbook remains unmodified.

---

## v1.0 — 17 August 2026 — Claude (Opus 5), research/evidence-audit agent

### Created
`article_type_assessment.md`, `literature_search_log.md`, `source_inventory.md`, `evidence_matrix.md`, `architecture_taxonomy.md`, `adaptation_chain_matrix.md`, `timescale_matrix.md`, `evidence_strength_matrix.md`, `novelty_boundary.md`, `absence_claims.md`, `manuscript_argument_map.md`, `unresolved_questions.md`, `draft_status.md`, `CHANGELOG.md`; `manuscript/01`–`10`; `audits/` × 7.

**No original research file was modified, renamed, moved or deleted. `Metasurface_Master_Research_Workbook_v1.xlsx` and the two context `.md` files were read only.**

### Scope changes

**CH-01 — Working title rejected.** *"From Instantaneous Shape Optimization to Two-Timescale Control"* implies the two-timescale move is this manuscript's contribution. **Evidence:** statistical-CSI FIM shape optimisation is published (arXiv:2512.23045, Kumar et al., Dec 2025; arXiv:2601.15471); a two-rate schedule — element movement per subframe, phase per time slot — is already in the FIM channel-estimation protocol in the corpus (YAN-25, pp. 7–8, verified verbatim); and instantaneous-versus-statistical CSI plus hierarchical control are stated in the movable-antenna survey (MA-26, pp. 20, 26–27, verified verbatim). **Recommended title:** *"What Has Actually Been Measured? Adaptation Timescales and Hardware Evidence for Flexible Intelligent Metasurfaces in High-Mobility ISAC."* Conservative alternative retained.

**CH-02 — Article type fixed as a structured critical review.** Not systematic, not systematic mapping. **Evidence:** no indexed-database search, no protocol, no recorded hit counts, no forward citation search, single screener. The extraction side is protocol-driven and that is what the manuscript claims.

**CH-03 — CST–Python study excluded from the review manuscript.** Gate criteria failed: no CST results exist yet, and the study has no time variable, which is the review's subject. Kept as a separate project. Its own position improved (CH-08).

**CH-04 — Contribution restated as four auditing contributions** (stage-resolved decomposition; timing-definition discipline; traceability audit; reporting framework). No hardware, algorithm or control architecture is proposed.

### Corrections to the prior project record

**CH-05.** "One wavelength = 10.8 mm at 28 GHz" was recorded as a derived quantity. **It is stated by the source** (ANJ-25, p. 6 conclusion, verbatim). Reclassified as direct.

**CH-06.** "Converges after about 10 iterations" was attributed to the multiuser FIM paper. **Wrong paper.** ANJ-25 caps iterations at 100 with a −30 dB fractional-decrease criterion and plots convergence over 0–50 iterations (p. 5); the ≈10-iteration figure belongs to MOR-26 (p. 3). Both records corrected in `evidence_matrix.md` E-10.

**CH-07.** The workbook's `S-ID` numbering is **offset** from the current filename numbering for four sources: file `03` is workbook `S07`, file `04` is `S06`, file `06` is `S04`, file `07` is `S03`. Reconciliation table added at `source_inventory.md` §4. This audit uses author-year keys and does not adopt the S-IDs.

### Corpus findings

**CH-08 — Four "missing" sources are present.** Workbook `12_Missing_Sources` items M01 (Harz element paper), M02 (cylindrical conformal RIS), M03 and M04 (the two conformal reflectarray papers from the Nanjing group) and M05 (wide-angle conformal active metasurface) are all in `Metasurfaces/`. M01's presence materially improves the CST project's baseline situation.

**CH-09 — A previously unrecorded source was identified.** `ars-19-215-2022.pdf` is Harz & Kleine-Ostmann, *Adv. Radio Sci.* **19**, 215–220 (2022) — the array-level 10 × 10 measured follow-up to the 2020 element paper, not a duplicate of it. Added as HAR-22 and placed in independence group IG-20 with HAR-20. Suggested rename recorded; **no file was renamed.**

**CH-10 — ⚠ A source listed in the project context is genuinely missing.** `23_FanLi_FISP_Supplementary_Information.pdf`, described in `Metasurface_Literature_Review_Context_v2.1.md` §5 as item #23 of a 23-file package, **is not in the folder** under any name. It contains the measurement protocol for the 16.76 ms interval. Recorded as blocking item U1 and brought to the user's attention in the run report.

**CH-11 — Corpus totals established.** 27 PDF files → 26 unique documents → **24 independent research contributions** after collapsing four independence groups (IG-1, IG-13, IG-20, IG-22).

### Substantive findings recorded for the first time in this project

**CH-12.** The Bai closed-loop control cycle — ≈0.25 s per cycle over 5–15 iterations, image-processing-bound — was not in the prior project record. It is the only demonstrated closed-loop geometry controller in the corpus and it is two to three orders of magnitude slower than the element response time the FIM literature cites.

**CH-13.** The Yang wall-clock algorithm runtime (≈4 ms to ≈100 ms; slowest benchmark ≈1.2 s; axis units confirmed by rendering the page) was recorded only as "runtime comparisons" previously. It is the only FIM-specific wall-clock computation measurement in the corpus.

**CH-14.** The Xu 51-symbol figure (correlation > 0.5 at 90 mph, 2.6 GHz) was not in the prior record. It is the corpus's only quantified high-mobility decorrelation window.

**CH-15 ★.** **The traceability finding.** An external FIM system paper's Table I attributes a 30 ms "Morphing Period" to the liquid-metal platform — that platform's *isolated-ribbon* figure, against its own ≈300 ms full-surface figure — and a 10 ms "Morphing Period" to the filamentary platform, which has no counterpart in that paper's main text (element < 0.07 s; system within 0.1 s; closed loop ≈0.25 s × 5–15). Reported as a **discrepancy requiring verification**, not an error, because the relevant supplementary material is unavailable. The two external papers exhibiting this share authors, so no generalisation is made.

> ⚠ **Historical entry — the stated reason is superseded.** The supplementary material *was* obtained on 17 August 2026 (CH-20), and the complete package contains no 10 ms figure. **The verdict is nevertheless unchanged**: still a discrepancy requiring verification, not an error, but now because the *citing* authors' derivation cannot be observed rather than because the cited paper was unavailable. The closed-loop figure quoted above as "≈0.25 s × 5–15" was also wrong — see CH-21. This entry is retained as a record of what was believed at the time; **do not cite it as current state.**

**CH-16.** None of the three closest reviews mentions FIMs. Ligature-normalised full-text search returns zero occurrences of "FIM" and "flexible intelligent metasurface" in all three.

**CH-17.** Two simplifications overturned: liquid-crystal switch-off (72 ms) exceeds a complete measured electronic loop on a flexible surface (16.76 ms), so "fast electronics, slow mechanics" is mechanism-conditional; and a single one-bit panel reports update times differing by 100× because control networks serialise.

### Fixes applied during self-review

- **A-FIX-1** (`architecture_audit.md`) — §5.4 now states the active/passive distinction rather than relying on §2.6.
- **E-FIX-1** (`evidence_audit.md`) — §7.1 now identifies the commanded-shape platforms as mechanical, so the capability list cannot read as an RF claim.
- **I-FIX-1** (`independence_audit.md`) — §4.5 notes that the four FIM system papers represent three independent lines.
- **I-FIX-2** (`independence_audit.md`) — §6.5 states the author overlap between the two external papers and correspondingly declines to generalise. Abstract wording softened to match.
- **C4** (`citation_audit.md`) — added the reflection-amplitude floor (> 0.4) and the aerofoil/QPSK/3.1 GHz detail of the video-link demonstration to the evidence matrix and §5.1.
- **C3 near-miss rule adopted** — no numerical claim may rest on a token appearing only in an extracted figure region; body text or a rendered page must confirm it. Applied to verify the 3.0–3.4 GHz band, the 270° phase range, and the "Running time [Second]" axis.

### Method notes for future agents

1. `pdftotext` is not installed on this machine; `pypdf` and `pymupdf` (installed during this run) work. Three Wiley PDFs are rasterised and require page rendering plus image reading.
2. **Extracted text uses ligatures** (`ﬂ`, `ﬁ`). Naive searching for "flexible", "microfluidic" or "negligible" returns false negatives. Normalise before searching. One source additionally spells "negligible" as "neglegible".
3. Shell `grep` behaved unreliably against these files in this environment; Python regex searching was used throughout and is recommended.
4. Figure axis tick labels appear in extracted text as bare numbers and are easily mistaken for reported values. Verify against body text or a rendered page.

### Not done in this run
Figures and tables were specified but not produced. References were not formatted. No forward citation search. No database search. No domain-expert review. The Excel workbook's `14_AI_Review_Log` and `15_Change_Log` sheets were **not** updated — corrections CH-05 through CH-10 should be carried into them by the user, since this audit does not modify original project files.
